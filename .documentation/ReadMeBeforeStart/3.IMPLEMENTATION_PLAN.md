# EcoSphere 小型系统原型 - 完整实现计划

**最后更新**: 2025-11-27  
**状态**: 待开始实现  
**优先级**: 高  
**项目阶段**: Phase 3 - Small System Prototype  
**版本**: 3.0 (根据团队反馈更新)

---

## 📋 目录

1. [项目概述](#项目概述)
2. [原型范围](#原型范围)
3. [技术栈](#技术栈)
4. [类图严格遵循原则](#类图严格遵循原则)
5. [核心类定义](#核心类定义)
6. [数据库设计](#数据库设计)
7. [后端实现](#后端实现)
8. [前端实现](#前端实现)
9. [实现步骤](#实现步骤)
10. [类图关系检查清单](#类图关系检查清单)
11. [参考文档](#参考文档)

---

## 项目概述

### 背景
EcoSphere是为SAIT的Green Building Technology Access Centre (GBTAC)开发的智能建筑分析系统。
系统通过传感器收集建筑的水、电、热能数据，存储在SQL Server数据库中，EcoSphere以只读方式访问这些数据，
生成可视化报告、预测分析和交互式仪表板。

### Phase 3要求（更新）
根据Phase 3文档要求和团队讨论，Prototype阶段需要实现：

**P0 - 必须完成（演示核心）**:
1. **登录模块** (Login) - 用户认证和会话管理
2. **用户管理模块** (User Management) - Admin添加/编辑用户（无注册功能）
3. **碳足迹追踪模块** (Carbon Footprint Tracking) - 非平凡用例
   - 支持从数据库自动计算（基于电力数据）
   - 支持用户上传电费账单临时计算（不持久化）

**P1 - 时间允许则实现**:
4. **电力仪表板模块** (Electricity Dashboard) - 基础数据展示

**P2 - Skeleton实现（框架搭建，功能未来实现）**:
- 其他Report模块（Water, Thermal）
- Forecast预测功能（需要真实算法，但prototype阶段不实现）
- Period Comparison功能
- 3D可视化

**关键要求**: 
- 所有实现必须严格遵循class-diagram-optimized.puml中定义的类结构
- 系统为**内部系统**，无注册功能，用户由Admin统一管理
- 碳足迹支持两种计算模式（数据库 + 临时上传）

---

## 原型范围

### 1. 用户登录与管理模块 (Login & User Management) - P0

**功能需求**:
- 用户登录界面（email/password）
- 身份验证和JWT token生成
- Session管理和权限检查
- 登录后根据角色跳转（Admin/TeamMember）
- **Admin用户管理功能**:
  - 添加新用户（createUser）
  - 编辑用户信息（editUser）
  - 分配角色和权限（assignRole, editUserPermissions）
  - 查看用户列表
- **重要**: 系统为内部系统，**无注册功能**，所有用户由Admin统一创建和管理

**涉及的类** (来自class-diagram-optimized.puml):
- `User` (抽象类) - 基础用户类
  - Attributes: id, firstName, lastName, email, password, role
  - Methods: createUser(), login(), logout()
- `Admin` (继承User) - 管理员
  - 额外Methods: addUser(), editUserPermissions(), assignRole()
- `TeamMember` (继承User) - 团队成员
  - Attributes: promptChat, permissions
  - Methods: getPermissions()
- `AccessControl` - 权限控制
  - Methods: checkPermission(), updateUserPermissions()

**Use Case参考**: 
- UC-001 (Login) - Phase2_extracted.md Page 11
- UC-002 (Manage Users) - usecase-diagram.puml

**序列图参考**: 所有序列图都以用户登录为前提

**实现优先级**: 🔴 P0 - 必须完成

---

### 2. 碳足迹追踪模块 (Carbon Footprint Tracking) - P0

**功能需求**:
- **模式1: 数据库自动计算**
  - 从数据库读取用户的电力消耗数据
  - 自动计算碳足迹（CO2排放量）
  - 数据持久化存储
  - 显示历史碳足迹趋势
  
- **模式2: 用户临时上传**
  - 用户上传电费账单数据（年、月、用电量）
  - 实时计算碳足迹
  - **数据不持久化**，关闭浏览器后清除
  - 用于用户快速估算个人碳足迹
  
- 数据可视化（图表展示）
- 碳足迹计算公式：CO2排放 = 用电量 × 碳排放因子（0.5 kg CO2/kWh）

**涉及的类** (来自class-diagram-optimized.puml):
- `CarbonFootprint` 类
  - Attributes: id, co2Emission, billEntries (List)
  - Methods: calculateFootprint(), addBillEntry(), overrideWithDIY()
- `BillEntry` 类
  - Attributes: id, year, month, electricityUsage
- `ElectricityReport` 类（关联）
  - 关系: CarbonFootprint "1" --> "1" ElectricityReport : calculates

**数据库设计**:
- CARBON_FOOTPRINT表（独立表，支持两种模式）
- 通过user_id关联用户
- 可选的report_id关联电力报告（模式1使用）
- 临时数据（模式2）仅存储在前端state，不写入数据库

**Use Case参考**: UC-005 (Carbon Footprint Calculation) - Phase2_extracted.md Page 14-15

**重要关系**: UI "1" *-- "1" CarbonFootprint : owns

**实现优先级**: 🔴 P0 - 必须完成（非平凡用例）

---

### 3. 电力仪表板模块 (Electricity Dashboard) - P1

**功能需求**:
- 实时显示电力消耗和发电数据
- 数据可视化（图表、仪表盘）
- 历史数据查询和时间段筛选
- **核心功能**: 在电力报告中集成显示碳足迹数据
- 基础的自给自足率计算
- **未来功能（Prototype阶段不实现）**:
  - 生成预测报告（Forecast）- 需要真实预测算法
  - 期间对比（YoY/PoP）
  - 导出报告功能（PDF）

**涉及的类** (来自class-diagram-optimized.puml):
- `ElectricityReport` 类 (继承Report)
  - Attributes: id, reportType, electricityConsumption, electricityGeneration
  - Methods: calculateCarbonFootprint(), generateHotspotMap(), generateCarbonVsConsumptionChart()
  - 继承Methods: exportReport(), filterReport()
- `Report` 类 (抽象类)
  - Attributes: id, reportType
  - Methods: exportReport(), filterReport()
- `Dashboard` 类
  - Attributes: id, dashboardType
  - Methods: generateView(), filterByPermissions()
- `Visualizer` 类
  - Attributes: id, visualizeType
  - Methods: generateGraph(), calculateSelfSufficiency(), renderHotspotMap()
- `Forecast` 类
  - Attributes: id
  - Methods: generateForecast()
- `PeriodComparison` 类
  - Attributes: id
  - Methods: comparePoP(), compareYoY(), compareCustom()
- `Filter` 类
  - Attributes: id, location, area, timePeriod
  - Methods: applyFilter(), resetToDefault()
- `ReportCustomization` 类
  - Attributes: id, includeGraphs
  - Methods: preview(), download()

**Use Case参考**: 
- UC-007 (View Dashboards) - Phase2_extracted.md Page 17-18
- UC-006 (Generate Forecast) - Phase2_extracted.md Page 15-16（未来实现）

**序列图参考**: deliverables/seq-electricity-dashboard.puml

**活动图参考**: deliverables/activity-electricity-dashboard.puml

**关键集成点**: 
- ElectricityReport必须调用CarbonFootprint.calculateFootprint()
- 报告中同时显示电力数据和碳足迹数据

**实现优先级**: 🟡 P1 - 时间允许则实现（基础版本）

---

## 技术栈

### 前端
- **框架**: React.js (Create React App或Vite)
- **UI库**: Material-UI 或 React Bootstrap
- **状态管理**: Redux 或 Context API
- **数据可视化**: Plotly.js, Chart.js, Recharts
- **路由**: React Router
- **HTTP客户端**: Axios

### 后端
- **运行时**: Node.js
- **框架**: Express.js
- **API设计**: RESTful API
- **认证**: JWT (JSON Web Tokens)
- **Session管理**: express-session

### 数据库
- **主数据库**: Microsoft SQL Server (GBTAC现有数据库，只读访问)
- **ORM**: Sequelize 或 Prisma
- **连接**: mssql driver for Node.js

### 开发工具
- **版本控制**: Git / GitHub
- **API测试**: Postman
- **数据库管理**: SQL Server Management Studio
- **UML工具**: PlantUML, Software Ideas Modeler
- **UI设计**: Figma

---

## 类图严格遵循原则

### ⚠️ 重要说明

**原型实现必须100%严格按照 `instructions/ref/class-diagram-optimized.puml` 中定义的类结构来实现。**

虽然实际开发中可能会有调整，但类图是实现的大纲和基础。所有类的attributes和methods必须与类图保持一致。

### 实现规则

1. **Attributes完整性**: 所有类必须包含类图中定义的所有attributes
2. **Methods签名一致**: 所有方法必须按照类图中的签名实现（参数和返回类型）
3. **关系实现**: 类之间的关系必须在代码中体现（继承、组合、聚合、依赖）
4. **命名一致性**: 方法和属性命名必须与类图保持一致
5. **包结构**: 遵循类图中的package组织结构

### 类图关键关系

```
User (抽象)
  ├── Admin (继承)
  └── TeamMember (继承)

Report (抽象)
  ├── WaterReport (继承)
  ├── ElectricityReport (继承)
  └── ThermalReport (继承)

CarbonFootprint "1" o-- "*" BillEntry (组合)
CarbonFootprint "1" --> "1" ElectricityReport (依赖)

UI "1" *-- "1" CarbonFootprint (组合)
UI "1" *-- "*" Dashboard (组合)
UI "1" o-- "*" Report (聚合)

Dashboard --> Visualizer (使用)
Dashboard --> Report (聚合)
Dashboard ..> AccessControl (依赖)

Visualizer --> Report (可视化)
Forecast --> Report (生成)
Report --> PeriodComparison (比较)
```

---

## 核心类定义

以下类定义直接来自 `class-diagram-optimized.puml`，必须严格遵循：

### 1. User类（抽象类）
**Package**: User Management

**Attributes**:
```
- id: int
- firstName: String
- lastName: String
- email: String
- password: String
- role: String
```

**Methods**:
```
+ createUser(id, ...): void
+ login(email, password): void
+ logout(): void
```

**子类**:
- `Admin` - 管理员，额外方法：addUser(), editUserPermissions(), assignRole(), createQuiz(), editQuiz(), deleteQuiz()
- `TeamMember` - 团队成员，额外属性：promptChat (int), permissions (List)，额外方法：getPermissions()

---

### 2. CarbonFootprint类
**Package**: Carbon Analysis

**Attributes**:
```
- id: int
- co2Emission: double
- billEntries: List
```

**Methods**:
```
+ calculateFootprint(...): double
+ addBillEntry(...): void
+ overrideWithDIY(): DataSet
```

**关系**:
- 组合: `CarbonFootprint "1" o-- "*" BillEntry : stores`
- 依赖: `CarbonFootprint "1" --> "1" ElectricityReport : calculates`
- 组合: `UI "1" *-- "1" CarbonFootprint : owns`

---

### 3. BillEntry类
**Package**: Carbon Analysis

**Attributes**:
```
- id: int
- year: int
- month: String
- electricityUsage: double
```

**关系**: 被CarbonFootprint包含

---

### 4. Report类（抽象类）
**Package**: Reporting System

**Attributes**:
```
- id: int
- reportType: String
```

**Methods**:
```
+ exportReport(): void
+ filterReport(...): void
```

**子类**: WaterReport, ElectricityReport, ThermalReport

**关系**:
- 依赖: `Report ..> Database : queries`
- 依赖: `Report ..> AccessControl : checks permission`
- 关联: `Forecast --> Report : generates`
- 关联: `Report --> PeriodComparison : compares`

---

### 5. ElectricityReport类
**Package**: Reporting System

**继承**: Report (抽象类)

**Attributes** (继承自Report):
```
- id: int
- reportType: String
```

**特有Attributes**:
```
- electricityConsumption: double
- electricityGeneration: double
```

**Methods**:
```
+ calculateCarbonFootprint(...): double
+ generateHotspotMap(...): Map
+ generateCarbonVsConsumptionChart(): Chart
```

**继承的Methods**:
```
+ exportReport(): void
+ filterReport(...): void
```

**关系**:
- 继承: `ElectricityReport --|> Report : is-a`
- 被计算: `CarbonFootprint "1" --> "1" ElectricityReport : calculates`

---

### 6. Dashboard类
**Package**: User Interface

**Attributes**:
```
- id: int
- dashboardType: String
```

**Methods**:
```
+ generateView(user, type): DashboardView
+ filterByPermissions(user): List
```

**关系**:
- 组合: `UI "1" *-- "*" Dashboard : displays`
- 使用: `Dashboard --> Visualizer : uses`
- 聚合: `Dashboard --> Report : aggregates`
- 依赖: `Dashboard ..> AccessControl : checks permission`

---

### 7. Visualizer类
**Package**: Visualization

**Attributes**:
```
- id: int
- visualizeType: String
```

**Methods**:
```
+ generateGraph(...): void
+ calculateSelfSufficiency(...): double
+ renderHotspotMap(...): Map
+ renderHeatMap(...): Map
+ renderWeatherWidget(...): Widget
```

**关系**:
- 可视化: `Visualizer --> Report : visualizes`
- 被使用: `Dashboard --> Visualizer : uses`

---

### 8. Forecast类
**Package**: Reporting System

**Attributes**:
```
- id: int
```

**Methods**:
```
+ generateForecast(...): DataSet
```

**关系**:
- 生成: `Forecast --> Report : generates`

---

### 9. PeriodComparison类
**Package**: Analytics

**Attributes**:
```
- id: int
```

**Methods**:
```
+ comparePoP(currentPeriod): double
+ compareYoY(currentPeriod): double
+ compareCustom(period1, period2): double
```

**关系**:
- 比较: `Report --> PeriodComparison : compares`

---

### 10. UI类
**Package**: User Interface

**Attributes**:
```
- currentView: String
```

**Methods**:
```
+ renderView(...): void
+ updateUI(...): void
+ displayWarning(...): void
```

**关系**:
- 组合: `UI "1" *-- "*" Dashboard : displays`
- 组合: `UI "1" *-- "1" Filter : owns`
- 组合: `UI "1" *-- "1" ReportCustomization : owns`
- 组合: `UI "1" *-- "1" CarbonFootprint : owns`
- 聚合: `UI "1" o-- "*" Report : displays`
- 依赖: `UI ..> AccessControl : checks permission`
- 被使用: `User --> UI : uses`

---

### 11. Filter类
**Package**: User Interface

**Attributes**:
```
- id: int
- location: String
- area: String
- timePeriod: String
```

**Methods**:
```
+ applyFilter(): DataSet
+ resetToDefault(): void
```

---

### 12. ReportCustomization类
**Package**: User Interface

**Attributes**:
```
- id: int
- includeGraphs: List
```

**Methods**:
```
+ preview(): ReportPreview
+ download(...): void
```

---

### 13. AccessControl类
**Package**: Security

**Methods**:
```
+ checkPermission(user, module): boolean
+ updateUserPermissions(userId, permissions): void
```

**关系**:
- 被使用: `Admin ..> AccessControl : uses`
- 被依赖: `UI ..> AccessControl : checks permission`
- 被依赖: `Dashboard ..> AccessControl : checks permission`
- 被依赖: `Report ..> AccessControl : checks permission`

---

### 14. Database接口
**Package**: Data Layer

**Methods**:
```
+ executeQuery(...): DataSet
+ fetchSensorData(): DataSet
```

**关系**:
- 被查询: `Report ..> Database : queries`

---

## 数据库设计

### 基于ERD图和类图的表结构

参考文档: `instructions/ref/erd-diagram.puml`

### 1. USERS表
```sql
CREATE TABLE USERS (
    id INT PRIMARY KEY IDENTITY(1,1),
    firstName NVARCHAR(100) NOT NULL,
    lastName NVARCHAR(100) NOT NULL,
    email NVARCHAR(255) NOT NULL UNIQUE,
    password NVARCHAR(255) NOT NULL,
    role NVARCHAR(50) NOT NULL CHECK (role IN ('Admin', 'TeamMember')),
    promptChat INT NULL,  -- 仅TeamMember使用
    permissions NVARCHAR(MAX) NULL,  -- JSON格式，仅TeamMember使用
    created_at DATETIME DEFAULT GETDATE(),
    updated_at DATETIME DEFAULT GETDATE()
);
```

### 2. REPORTS表（基础表）
```sql
CREATE TABLE REPORTS (
    id INT PRIMARY KEY IDENTITY(1,1),
    user_id INT NOT NULL FOREIGN KEY REFERENCES USERS(id),
    report_type NVARCHAR(50) NOT NULL CHECK (report_type IN ('Water', 'Electricity', 'Thermal')),
    created_at DATETIME DEFAULT GETDATE(),
    updated_at DATETIME DEFAULT GETDATE()
);
```

### 3. ELECTRICITY_REPORTS表
```sql
CREATE TABLE ELECTRICITY_REPORTS (
    id INT PRIMARY KEY FOREIGN KEY REFERENCES REPORTS(id),
    electricityConsumption DECIMAL(10,2) NOT NULL,
    electricityGeneration DECIMAL(10,2) NOT NULL,
    carbonFootprint DECIMAL(10,2) NULL,  -- 关联碳足迹计算结果
    selfSufficiency DECIMAL(5,2) NULL
);
```

### 4. CARBON_FOOTPRINT表（独立表设计）
```sql
CREATE TABLE CARBON_FOOTPRINT (
    id INT PRIMARY KEY IDENTITY(1,1),
    user_id INT NOT NULL FOREIGN KEY REFERENCES USERS(id),
    report_id INT NULL FOREIGN KEY REFERENCES ELECTRICITY_REPORTS(id),  -- 可选，用于关联电力报告
    co2Emission DECIMAL(10,2) NOT NULL,
    calculation_source NVARCHAR(50) NOT NULL CHECK (calculation_source IN ('database', 'user_upload')),  -- 计算来源
    created_at DATETIME DEFAULT GETDATE(),
    updated_at DATETIME DEFAULT GETDATE()
);
```

**设计说明**:
- `user_id`: 关联用户
- `report_id`: 可选，用于关联电力报告（模式1：数据库自动计算）
- `calculation_source`: 标识数据来源
  - 'database': 从数据库电力数据自动计算
  - 'user_upload': 用户临时上传（实际不会存储到数据库，此字段用于未来扩展）
- 独立表设计符合数据库规范化原则

### 5. BILL_ENTRY表
```sql
CREATE TABLE BILL_ENTRY (
    id INT PRIMARY KEY IDENTITY(1,1),
    carbonFootprintId INT NOT NULL FOREIGN KEY REFERENCES CARBON_FOOTPRINT(id),
    year INT NOT NULL,
    month NVARCHAR(20) NOT NULL,
    electricityUsage DECIMAL(10,2) NOT NULL,
    created_at DATETIME DEFAULT GETDATE()
);
```

**设计说明**:
- 存储用户的电费账单条目
- 用于碳足迹计算的基础数据
- 支持历史数据追踪

### 6. HISTORY_LOGS表
```sql
CREATE TABLE HISTORY_LOGS (
    id INT PRIMARY KEY IDENTITY(1,1),
    user_id INT NOT NULL FOREIGN KEY REFERENCES USERS(id),
    log_type NVARCHAR(50) NOT NULL CHECK (log_type IN ('report', 'ai_query')),
    timestamp DATETIME DEFAULT GETDATE(),
    details NVARCHAR(MAX) NULL
);
```

### 7. REPORT_HISTORY表
```sql
CREATE TABLE REPORT_HISTORY (
    id INT PRIMARY KEY FOREIGN KEY REFERENCES HISTORY_LOGS(id),
    report_id INT NOT NULL FOREIGN KEY REFERENCES REPORTS(id),
    filter_location NVARCHAR(100) NULL,
    filter_area NVARCHAR(100) NULL,
    filter_time_period NVARCHAR(100) NULL
);
```

### 测试数据脚本

```sql
-- 插入测试用户
INSERT INTO USERS (firstName, lastName, email, password, role) VALUES
('Admin', 'User', 'admin@sait.ca', '$2b$10$hashedpassword', 'Admin'),
('John', 'Doe', 'john.doe@sait.ca', '$2b$10$hashedpassword', 'TeamMember');

-- 插入测试碳足迹数据
INSERT INTO CARBON_FOOTPRINT (user_id, co2Emission) VALUES (2, 1250.50);

-- 插入测试账单条目
INSERT INTO BILL_ENTRY (carbonFootprintId, year, month, electricityUsage) VALUES
(1, 2024, 'January', 500.0),
(1, 2024, 'February', 450.0),
(1, 2024, 'March', 480.0);

-- 插入测试报告
INSERT INTO REPORTS (user_id, report_type) VALUES (2, 'Electricity');

-- 插入测试电力报告
INSERT INTO ELECTRICITY_REPORTS (id, electricityConsumption, electricityGeneration, carbonFootprint) 
VALUES (1, 2500.0, 1800.0, 1250.50);
```

---

## 后端实现

### 项目结构

```
backend/
├── src/
│   ├── models/           # 类图对应的模型类
│   │   ├── User.js
│   │   ├── Admin.js
│   │   ├── TeamMember.js
│   │   ├── CarbonFootprint.js
│   │   ├── BillEntry.js
│   │   ├── Report.js
│   │   ├── ElectricityReport.js
│   │   ├── Dashboard.js
│   │   ├── Visualizer.js
│   │   ├── Forecast.js
│   │   ├── PeriodComparison.js
│   │   ├── Filter.js
│   │   ├── ReportCustomization.js
│   │   └── AccessControl.js
│   ├── controllers/      # 控制器
│   │   ├── authController.js
│   │   ├── carbonFootprintController.js
│   │   ├── electricityReportController.js
│   │   └── dashboardController.js
│   ├── routes/           # 路由定义
│   │   ├── auth.js
│   │   ├── carbonFootprint.js
│   │   ├── electricityReport.js
│   │   └── dashboard.js
│   ├── middleware/       # 中间件
│   │   ├── auth.js
│   │   └── errorHandler.js
│   ├── config/           # 配置
│   │   └── database.js
│   └── app.js            # 主应用
├── package.json
└── .env
```

### 核心类实现示例

#### User类（抽象类）
```javascript
// models/User.js
class User {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.firstName = '';
    this.lastName = '';
    this.email = '';
    this.password = '';
    this.role = '';
  }

  // Methods from class diagram
  async createUser(id, firstName, lastName, email, password, role) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.password = password;
    this.role = role;
    // Database operation
  }

  async login(email, password) {
    // Validation logic
    // Return user object
  }

  async logout() {
    // Logout logic
  }
}

module.exports = User;
```

#### CarbonFootprint类（支持两种模式）
```javascript
// models/CarbonFootprint.js
class CarbonFootprint {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.co2Emission = 0.0;
    this.billEntries = [];  // List of BillEntry objects
    this.calculationSource = 'database';  // 'database' or 'user_upload'
  }

  // Methods from class diagram
  async calculateFootprint(electricityUsage, emissionFactor) {
    // Calculate: CO2 = electricity × emission factor
    // 标准碳排放因子: 0.5 kg CO2/kWh
    this.co2Emission = electricityUsage * emissionFactor;
    return this.co2Emission;  // Returns double
  }

  async addBillEntry(year, month, electricityUsage, persist = true) {
    const billEntry = new BillEntry();
    billEntry.year = year;
    billEntry.month = month;
    billEntry.electricityUsage = electricityUsage;
    this.billEntries.push(billEntry);
    
    // 根据persist参数决定是否保存到数据库
    if (persist) {
      // 模式1: 保存到数据库
      await this.saveBillEntryToDatabase(billEntry);
    }
    // 模式2: 仅保存在内存中，不持久化
  }

  async overrideWithDIY() {
    // DIY data override logic - 用户临时上传模式
    // 设置为临时数据，不持久化
    this.calculationSource = 'user_upload';
    return {};  // Returns DataSet
  }

  async calculateFromDatabase(userId) {
    // 模式1: 从数据库读取电力数据并计算
    // 1. 查询用户的电力报告
    // 2. 获取总用电量
    // 3. 计算碳足迹
    // 4. 保存到数据库
    this.calculationSource = 'database';
  }

  async calculateFromUserUpload(billEntries) {
    // 模式2: 基于用户上传的临时数据计算
    // 1. 接收用户上传的账单数据
    // 2. 计算总用电量
    // 3. 计算碳足迹
    // 4. 不保存到数据库
    this.calculationSource = 'user_upload';
    this.billEntries = billEntries;
    
    const totalUsage = billEntries.reduce((sum, entry) => 
      sum + entry.electricityUsage, 0
    );
    
    return await this.calculateFootprint(totalUsage, 0.5);
  }
}

module.exports = CarbonFootprint;
```

#### BillEntry类
```javascript
// models/BillEntry.js
class BillEntry {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.year = 0;
    this.month = '';
    this.electricityUsage = 0.0;
  }
}

module.exports = BillEntry;
```

#### ElectricityReport类
```javascript
// models/ElectricityReport.js
const Report = require('./Report');

class ElectricityReport extends Report {
  constructor() {
    super();
    // Inherited attributes
    this.reportType = 'Electricity';
    
    // Specific attributes from class diagram
    this.electricityConsumption = 0.0;
    this.electricityGeneration = 0.0;
  }

  // Methods from class diagram
  async calculateCarbonFootprint(emissionFactor) {
    // 关键: 调用CarbonFootprint类计算
    const carbonFootprint = this.electricityConsumption * emissionFactor;
    return carbonFootprint;  // Returns double
  }

  async generateHotspotMap(data) {
    // Generate hotspot map logic
    return {};  // Returns Map
  }

  async generateCarbonVsConsumptionChart() {
    // Generate chart data
    return {
      labels: [],
      carbonData: [],
      consumptionData: []
    };  // Returns Chart
  }

  // Inherited methods from Report
  // exportReport() and filterReport() are inherited
}

module.exports = ElectricityReport;
```

#### Report基类
```javascript
// models/Report.js
class Report {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.reportType = '';
  }

  // Methods from class diagram
  async exportReport() {
    // Export report logic
  }

  async filterReport(filters) {
    // Filter report logic
  }
}

module.exports = Report;
```

#### Dashboard类
```javascript
// models/Dashboard.js
const AccessControl = require('./AccessControl');

class Dashboard {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.dashboardType = '';
  }

  // Methods from class diagram
  async generateView(user, type) {
    // Check permission (Dashboard ..> AccessControl)
    const hasPermission = await AccessControl.checkPermission(user, 'dashboard');
    if (!hasPermission) {
      throw new Error('Permission denied');
    }

    // Generate view
    return {
      dashboardType: type,
      data: {}
    };  // Returns DashboardView
  }

  async filterByPermissions(user) {
    // Filter data based on user permissions
    return [];  // Returns List
  }
}

module.exports = Dashboard;
```

#### Visualizer类
```javascript
// models/Visualizer.js
class Visualizer {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.visualizeType = '';
  }

  // Methods from class diagram
  generateGraph(data, graphType) {
    // Generate graph logic
    // Returns void
  }

  calculateSelfSufficiency(generation, consumption) {
    return (generation / consumption) * 100;  // Returns double
  }

  renderHotspotMap(data) {
    // Render hotspot map
    return {};  // Returns Map
  }

  renderHeatMap(data) {
    // Render heat map
    return {};  // Returns Map
  }

  renderWeatherWidget(weatherData) {
    // Render weather widget
    return {};  // Returns Widget
  }
}

module.exports = Visualizer;
```

#### Forecast类
```javascript
// models/Forecast.js
class Forecast {
  constructor() {
    // Attributes from class diagram
    this.id = null;
  }

  // Methods from class diagram
  async generateForecast(historicalData, duration) {
    // Analyze patterns and predict future
    // Use linear regression or time series analysis
    return {};  // Returns DataSet
  }
}

module.exports = Forecast;
```

#### PeriodComparison类
```javascript
// models/PeriodComparison.js
class PeriodComparison {
  constructor() {
    // Attributes from class diagram
    this.id = null;
  }

  // Methods from class diagram
  async comparePoP(currentPeriod) {
    // Period-over-Period comparison
    return 0.0;  // Returns double (percentage change)
  }

  async compareYoY(currentPeriod) {
    // Year-over-Year comparison
    return 0.0;  // Returns double (percentage change)
  }

  async compareCustom(period1, period2) {
    // Custom period comparison
    return 0.0;  // Returns double (percentage change)
  }
}

module.exports = PeriodComparison;
```

#### AccessControl类
```javascript
// models/AccessControl.js
class AccessControl {
  // Static methods from class diagram
  static async checkPermission(user, module) {
    // Check user permission based on role
    if (user.role === 'Admin') {
      return true;
    }
    
    if (user.role === 'TeamMember') {
      // Check permissions list
      return user.permissions && user.permissions.includes(module);
    }
    
    return false;  // Returns boolean
  }

  static async updateUserPermissions(userId, permissions) {
    // Update user permissions in database
    // Returns void
  }
}

module.exports = AccessControl;
```

#### Database接口
```javascript
// interfaces/Database.js
const sql = require('mssql');

class Database {
  async executeQuery(query, params) {
    // Execute SQL query
    const result = await sql.query(query);
    return result.recordset;  // Returns DataSet
  }

  async fetchSensorData() {
    // Fetch latest sensor data
    const query = 'SELECT TOP 100 * FROM sensor_data ORDER BY ts DESC';
    return await this.executeQuery(query);  // Returns DataSet
  }
}

module.exports = Database;
```

### API端点设计（严格对应类图方法）

#### 1. User认证API
```javascript
// routes/auth.js
POST /api/user/create      // User.createUser()
POST /api/user/login       // User.login()
POST /api/user/logout      // User.logout()
```

#### 2. CarbonFootprint API
```javascript
// routes/carbonFootprint.js
GET  /api/carbon-footprint/:id              // 获取碳足迹数据
POST /api/carbon-footprint/calculate        // CarbonFootprint.calculateFootprint()
POST /api/carbon-footprint/bill-entry       // CarbonFootprint.addBillEntry()
PUT  /api/carbon-footprint/override         // CarbonFootprint.overrideWithDIY()
```

#### 3. ElectricityReport API
```javascript
// routes/electricityReport.js
GET  /api/electricity/report/:id                    // 获取电力报告
POST /api/electricity/report/create                 // 创建电力报告
POST /api/electricity/report/calculate-carbon       // ElectricityReport.calculateCarbonFootprint()
GET  /api/electricity/report/hotspot-map/:id        // ElectricityReport.generateHotspotMap()
GET  /api/electricity/report/carbon-chart/:id       // ElectricityReport.generateCarbonVsConsumptionChart()
POST /api/electricity/report/export/:id             // Report.exportReport()
POST /api/electricity/report/filter                 // Report.filterReport()
```

#### 4. Dashboard API
```javascript
// routes/dashboard.js
GET /api/dashboard/view                    // Dashboard.generateView()
GET /api/dashboard/filter-permissions      // Dashboard.filterByPermissions()
```

#### 5. Visualizer API
```javascript
// routes/visualizer.js
POST /api/visualizer/graph                 // Visualizer.generateGraph()
GET  /api/visualizer/self-sufficiency      // Visualizer.calculateSelfSufficiency()
GET  /api/visualizer/hotspot-map           // Visualizer.renderHotspotMap()
```

#### 6. Forecast API
```javascript
// routes/forecast.js
POST /api/forecast/generate                // Forecast.generateForecast()
```

#### 7. PeriodComparison API
```javascript
// routes/comparison.js
GET /api/comparison/pop                    // PeriodComparison.comparePoP()
GET /api/comparison/yoy                    // PeriodComparison.compareYoY()
POST /api/comparison/custom                // PeriodComparison.compareCustom()
```

---

## 前端实现

### 项目结构

```
frontend/
├── src/
│   ├── components/       # React组件
│   │   ├── Login/
│   │   │   └── LoginPage.jsx
│   │   ├── Dashboard/
│   │   │   ├── DashboardLayout.jsx
│   │   │   └── ElectricityDashboard.jsx
│   │   ├── CarbonFootprint/
│   │   │   └── CarbonFootprintPage.jsx
│   │   └── Common/
│   │       ├── Header.jsx
│   │       ├── Sidebar.jsx
│   │       └── Chart.jsx
│   ├── services/         # API服务（对应类图的类）
│   │   ├── UserService.js
│   │   ├── CarbonFootprintService.js
│   │   ├── ElectricityReportService.js
│   │   ├── DashboardService.js
│   │   └── VisualizerService.js
│   ├── store/            # Redux状态管理
│   │   ├── userSlice.js
│   │   ├── dashboardSlice.js
│   │   └── store.js
│   ├── utils/            # 工具函数
│   │   └── api.js
│   ├── App.js
│   └── index.js
├── package.json
└── .env
```

### UI类实现（使用Context API）

根据类图，UI类需要实现以下结构：
```
UI类:
- currentView: String
+ renderView(...): void
+ updateUI(...): void
+ displayWarning(...): void

关系:
- UI "1" *-- "*" Dashboard : displays
- UI "1" *-- "1" Filter : owns
- UI "1" *-- "1" ReportCustomization : owns
- UI "1" *-- "1" CarbonFootprint : owns
- UI "1" o-- "*" Report : displays
```

**实现方案**: 使用React Context API

#### UIContext.js（UI类的React实现）
```javascript
// contexts/UIContext.js
import React, { createContext, useContext, useState } from 'react';
import DashboardService from '../services/DashboardService';
import CarbonFootprintService from '../services/CarbonFootprintService';
import FilterService from '../services/FilterService';
import ReportCustomizationService from '../services/ReportCustomizationService';

class UI {
  constructor() {
    // Attributes from class diagram
    this.currentView = '';
    
    // Composition relationships (UI owns these)
    this.dashboard = null;  // UI "1" *-- "*" Dashboard
    this.filter = null;     // UI "1" *-- "1" Filter
    this.reportCustomization = null;  // UI "1" *-- "1" ReportCustomization
    this.carbonFootprint = null;  // UI "1" *-- "1" CarbonFootprint
    
    // Aggregation relationships (UI displays these)
    this.reports = [];  // UI "1" o-- "*" Report
    
    // UI state
    this.warningMessage = '';
    this.isLoading = false;
  }

  // Methods from class diagram
  renderView(viewName) {
    this.currentView = viewName;
    console.log(`Rendering view: ${viewName}`);
  }

  updateUI(data) {
    // Update UI state with new data
    console.log('Updating UI with data:', data);
  }

  displayWarning(message) {
    this.warningMessage = message;
    console.log(`Warning: ${message}`);
  }
}

// Create Context
const UIContext = createContext();

// Provider Component
export const UIProvider = ({ children }) => {
  const [ui] = useState(() => new UI());
  const [currentView, setCurrentView] = useState('');
  const [warningMessage, setWarningMessage] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  // Wrap UI methods to update React state
  const renderView = (viewName) => {
    ui.renderView(viewName);
    setCurrentView(viewName);
  };

  const updateUI = (data) => {
    ui.updateUI(data);
    // Trigger re-render if needed
  };

  const displayWarning = (message) => {
    ui.displayWarning(message);
    setWarningMessage(message);
    
    // Auto-clear warning after 5 seconds
    setTimeout(() => setWarningMessage(''), 5000);
  };

  const value = {
    ui,
    currentView,
    warningMessage,
    isLoading,
    setIsLoading,
    renderView,
    updateUI,
    displayWarning
  };

  return <UIContext.Provider value={value}>{children}</UIContext.Provider>;
};

// Custom Hook
export const useUI = () => {
  const context = useContext(UIContext);
  if (!context) {
    throw new Error('useUI must be used within UIProvider');
  }
  return context;
};

export default UIContext;
```

**使用示例**:
```javascript
// App.js
import { UIProvider } from './contexts/UIContext';

function App() {
  return (
    <UIProvider>
      <BrowserRouter>
        <Routes>
          {/* ... routes */}
        </Routes>
      </BrowserRouter>
    </UIProvider>
  );
}

// 在组件中使用
import { useUI } from '../contexts/UIContext';

function SomeComponent() {
  const { renderView, displayWarning, currentView } = useUI();
  
  useEffect(() => {
    renderView('Dashboard');
  }, []);
  
  const handleError = () => {
    displayWarning('Something went wrong!');
  };
  
  return <div>Current View: {currentView}</div>;
}
```

**设计优势**:
1. ✅ 完全符合类图定义（attributes和methods）
2. ✅ 实现了组合关系（owns Dashboard, Filter, etc.）
3. ✅ 轻量级，适合原型开发
4. ✅ 易于测试和维护
5. ✅ 符合React最佳实践

---

### 前端服务类（对应类图）

#### UserService.js
```javascript
// services/UserService.js
import api from '../utils/api';

class UserService {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.firstName = '';
    this.lastName = '';
    this.email = '';
    this.password = '';
    this.role = '';
  }

  // Methods from class diagram
  async createUser(id, firstName, lastName, email, password, role) {
    const response = await api.post('/api/user/create', {
      id, firstName, lastName, email, password, role
    });
    return response.data;
  }

  async login(email, password) {
    const response = await api.post('/api/user/login', { email, password });
    const user = response.data;
    
    // Store user data
    this.id = user.id;
    this.firstName = user.firstName;
    this.lastName = user.lastName;
    this.email = user.email;
    this.role = user.role;
    
    return user;
  }

  async logout() {
    await api.post('/api/user/logout');
    // Clear user data
    this.id = null;
    this.firstName = '';
    this.lastName = '';
    this.email = '';
    this.role = '';
  }
}

export default new UserService();
```

#### CarbonFootprintService.js
```javascript
// services/CarbonFootprintService.js
import api from '../utils/api';

class CarbonFootprintService {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.co2Emission = 0.0;
    this.billEntries = [];
  }

  // Methods from class diagram
  async calculateFootprint(electricityUsage, emissionFactor) {
    const response = await api.post('/api/carbon-footprint/calculate', {
      electricityUsage,
      emissionFactor
    });
    this.co2Emission = response.data.co2Emission;
    return this.co2Emission;  // Returns double
  }

  async addBillEntry(year, month, electricityUsage) {
    const response = await api.post('/api/carbon-footprint/bill-entry', {
      year, month, electricityUsage
    });
    return response.data;
  }

  async overrideWithDIY() {
    const response = await api.put('/api/carbon-footprint/override');
    return response.data;  // Returns DataSet
  }

  async getCarbonFootprint(id) {
    const response = await api.get(`/api/carbon-footprint/${id}`);
    this.id = response.data.id;
    this.co2Emission = response.data.co2Emission;
    this.billEntries = response.data.billEntries;
    return response.data;
  }
}

export default new CarbonFootprintService();
```

#### ElectricityReportService.js
```javascript
// services/ElectricityReportService.js
import api from '../utils/api';

class ElectricityReportService {
  constructor() {
    // Attributes from class diagram (inherited from Report)
    this.id = null;
    this.reportType = 'Electricity';
    
    // Specific attributes
    this.electricityConsumption = 0.0;
    this.electricityGeneration = 0.0;
  }

  // Methods from class diagram
  async calculateCarbonFootprint(emissionFactor) {
    const response = await api.post('/api/electricity/report/calculate-carbon', {
      consumption: this.electricityConsumption,
      emissionFactor
    });
    return response.data.carbonFootprint;  // Returns double
  }

  async generateHotspotMap(data) {
    const response = await api.get('/api/electricity/report/hotspot-map/' + this.id);
    return response.data;  // Returns Map
  }

  async generateCarbonVsConsumptionChart() {
    const response = await api.get('/api/electricity/report/carbon-chart/' + this.id);
    return response.data;  // Returns Chart
  }

  // Inherited methods from Report
  async exportReport() {
    const response = await api.post('/api/electricity/report/export/' + this.id);
    return response.data;
  }

  async filterReport(filters) {
    const response = await api.post('/api/electricity/report/filter', filters);
    return response.data;
  }

  async getReport(userId) {
    const response = await api.get(`/api/electricity/report/${userId}`);
    this.id = response.data.id;
    this.reportType = response.data.reportType;
    this.electricityConsumption = response.data.electricityConsumption;
    this.electricityGeneration = response.data.electricityGeneration;
    return response.data;
  }
}

export default new ElectricityReportService();
```

#### DashboardService.js
```javascript
// services/DashboardService.js
import api from '../utils/api';

class DashboardService {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.dashboardType = '';
  }

  // Methods from class diagram
  async generateView(user, type) {
    const response = await api.get('/api/dashboard/view', {
      params: { userId: user.id, type }
    });
    return response.data;  // Returns DashboardView
  }

  async filterByPermissions(user) {
    const response = await api.get('/api/dashboard/filter-permissions', {
      params: { userId: user.id }
    });
    return response.data;  // Returns List
  }
}

export default new DashboardService();
```

#### VisualizerService.js
```javascript
// services/VisualizerService.js
import api from '../utils/api';

class VisualizerService {
  constructor() {
    // Attributes from class diagram
    this.id = null;
    this.visualizeType = '';
  }

  // Methods from class diagram
  async generateGraph(data, graphType) {
    const response = await api.post('/api/visualizer/graph', {
      data, graphType
    });
    return response.data;
  }

  async calculateSelfSufficiency(generation, consumption) {
    const response = await api.get('/api/visualizer/self-sufficiency', {
      params: { generation, consumption }
    });
    return response.data.selfSufficiency;  // Returns double
  }

  async renderHotspotMap(data) {
    const response = await api.get('/api/visualizer/hotspot-map', {
      params: { data }
    });
    return response.data;  // Returns Map
  }
}

export default new VisualizerService();
```

### React组件实现

#### LoginPage.jsx
```jsx
// components/Login/LoginPage.jsx
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import UserService from '../../services/UserService';

const LoginPage = () => {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const navigate = useNavigate();

  const handleLogin = async (e) => {
    e.preventDefault();
    try {
      // Call User.login() method from class diagram
      const user = await UserService.login(email, password);
      
      // Store user info (all User attributes)
      localStorage.setItem('user', JSON.stringify({
        id: user.id,
        firstName: user.firstName,
        lastName: user.lastName,
        email: user.email,
        role: user.role
      }));
      
      // Navigate based on role
      if (user.role === 'Admin') {
        navigate('/admin/dashboard');
      } else {
        navigate('/dashboard');
      }
    } catch (err) {
      setError('Invalid email or password');
    }
  };

  return (
    <div className="login-container">
      <h1>EcoSphere Login</h1>
      <form onSubmit={handleLogin}>
        <div>
          <label>Email:</label>
          <input 
            type="email" 
            value={email} 
            onChange={(e) => setEmail(e.target.value)}
            required
          />
        </div>
        <div>
          <label>Password:</label>
          <input 
            type="password" 
            value={password} 
            onChange={(e) => setPassword(e.target.value)}
            required
          />
        </div>
        {error && <div className="error">{error}</div>}
        <button type="submit">Login</button>
      </form>
    </div>
  );
};

export default LoginPage;
```

#### CarbonFootprintPage.jsx
```jsx
// components/CarbonFootprint/CarbonFootprintPage.jsx
import React, { useState, useEffect } from 'react';
import CarbonFootprintService from '../../services/CarbonFootprintService';
import VisualizerService from '../../services/VisualizerService';
import { Line } from 'react-chartjs-2';

const CarbonFootprintPage = () => {
  const [carbonFootprint, setCarbonFootprint] = useState({
    id: null,
    co2Emission: 0,
    billEntries: []
  });
  const [chartData, setChartData] = useState(null);

  useEffect(() => {
    loadCarbonFootprint();
  }, []);

  const loadCarbonFootprint = async () => {
    const user = JSON.parse(localStorage.getItem('user'));
    const data = await CarbonFootprintService.getCarbonFootprint(user.id);
    setCarbonFootprint(data);
    
    // Generate visualization
    const chart = await VisualizerService.generateGraph(data, 'line');
    setChartData(chart);
  };

  const handleCalculate = async () => {
    // Calculate total electricity usage from bill entries
    const totalUsage = carbonFootprint.billEntries.reduce(
      (sum, entry) => sum + entry.electricityUsage, 0
    );
    
    // Call CarbonFootprint.calculateFootprint() method
    const emissionFactor = 0.5; // kg CO2 per kWh
    const result = await CarbonFootprintService.calculateFootprint(
      totalUsage, 
      emissionFactor
    );
    
    setCarbonFootprint(prev => ({ ...prev, co2Emission: result }));
  };

  const handleAddBillEntry = async (year, month, electricityUsage) => {
    // Call CarbonFootprint.addBillEntry() method
    await CarbonFootprintService.addBillEntry(year, month, electricityUsage);
    loadCarbonFootprint();
  };

  return (
    <div className="carbon-footprint-page">
      <h1>Carbon Footprint Tracking</h1>
      
      {/* Display CarbonFootprint attributes */}
      <div className="carbon-summary">
        <h2>Summary</h2>
        <p>ID: {carbonFootprint.id}</p>
        <p>Total CO2 Emission: {carbonFootprint.co2Emission.toFixed(2)} kg</p>
      </div>

      {/* Display BillEntry list */}
      <div className="bill-entries">
        <h2>Bill Entries</h2>
        <table>
          <thead>
            <tr>
              <th>Year</th>
              <th>Month</th>
              <th>Electricity Usage (kWh)</th>
            </tr>
          </thead>
          <tbody>
            {carbonFootprint.billEntries.map(entry => (
              <tr key={entry.id}>
                <td>{entry.year}</td>
                <td>{entry.month}</td>
                <td>{entry.electricityUsage}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      {/* Visualization */}
      {chartData && (
        <div className="chart-container">
          <h2>Carbon Footprint Trend</h2>
          <Line data={chartData} />
        </div>
      )}

      <button onClick={handleCalculate}>Calculate Carbon Footprint</button>
    </div>
  );
};

export default CarbonFootprintPage;
```

#### ElectricityDashboardPage.jsx
```jsx
// components/Dashboard/ElectricityDashboardPage.jsx
import React, { useState, useEffect } from 'react';
import ElectricityReportService from '../../services/ElectricityReportService';
import DashboardService from '../../services/DashboardService';
import CarbonFootprintService from '../../services/CarbonFootprintService';
import VisualizerService from '../../services/VisualizerService';
import { Line, Bar } from 'react-chartjs-2';

const ElectricityDashboardPage = () => {
  const [report, setReport] = useState({
    id: null,
    reportType: 'Electricity',
    electricityConsumption: 0,
    electricityGeneration: 0
  });
  const [carbonFootprint, setCarbonFootprint] = useState(0);
  const [carbonChartData, setCarbon ChartData] = useState(null);
  const [selfSufficiency, setSelfSufficiency] = useState(0);

  useEffect(() => {
    loadDashboard();
  }, []);

  const loadDashboard = async () => {
    const user = JSON.parse(localStorage.getItem('user'));
    
    // Call Dashboard.generateView() method
    const dashboardView = await DashboardService.generateView(user, 'Electricity');
    
    // Load electricity report data
    const reportData = await ElectricityReportService.getReport(user.id);
    setReport(reportData);
    
    // Call ElectricityReport.calculateCarbonFootprint() method
    const emissionFactor = 0.5; // kg CO2 per kWh
    const carbon = await ElectricityReportService.calculateCarbonFootprint(emissionFactor);
    setCarbonFootprint(carbon);
    
    // Call ElectricityReport.generateCarbonVsConsumptionChart() method
    const chart = await ElectricityReportService.generateCarbonVsConsumptionChart();
    setCarbonChartData(chart);
    
    // Call Visualizer.calculateSelfSufficiency() method
    const sufficiency = await VisualizerService.calculateSelfSufficiency(
      reportData.electricityGeneration,
      reportData.electricityConsumption
    );
    setSelfSufficiency(sufficiency);
  };

  const handleExport = async () => {
    // Call ElectricityReport.exportReport() method (inherited from Report)
    await ElectricityReportService.exportReport();
    alert('Report exported successfully!');
  };

  const handleGenerateHotspotMap = async () => {
    // Call ElectricityReport.generateHotspotMap() method
    const map = await ElectricityReportService.generateHotspotMap();
    // Display map
  };

  return (
    <div className="electricity-dashboard">
      <h1>Electricity Dashboard</h1>
      
      {/* Display ElectricityReport attributes */}
      <div className="report-summary">
        <h2>Report Details</h2>
        <p>Report ID: {report.id}</p>
        <p>Type: {report.reportType}</p>
        <p>Consumption: {report.electricityConsumption} kWh</p>
        <p>Generation: {report.electricityGeneration} kWh</p>
        <p>Self-Sufficiency: {selfSufficiency.toFixed(2)}%</p>
      </div>

      {/* 关键: 显示碳足迹数据（遵循类图关系） */}
      <div className="carbon-footprint-section">
        <h2>Carbon Footprint</h2>
        <p>CO2 Emission: {carbonFootprint.toFixed(2)} kg</p>
        <p className="note">
          * Carbon footprint calculated based on electricity consumption
        </p>
      </div>

      {/* Carbon vs Consumption Chart */}
      {carbonChartData && (
        <div className="chart-container">
          <h2>Carbon Emission vs Electricity Consumption</h2>
          <Line data={carbonChartData} />
        </div>
      )}

      {/* Action buttons */}
      <div className="actions">
        <button onClick={handleExport}>Export Report (PDF)</button>
        <button onClick={handleGenerateHotspotMap}>Generate Hotspot Map</button>
      </div>
    </div>
  );
};

export default ElectricityDashboardPage;
```

### 路由配置

```jsx
// App.js
import React from 'react';
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import LoginPage from './components/Login/LoginPage';
import DashboardLayout from './components/Dashboard/DashboardLayout';
import ElectricityDashboardPage from './components/Dashboard/ElectricityDashboardPage';
import CarbonFootprintPage from './components/CarbonFootprint/CarbonFootprintPage';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<LoginPage />} />
        <Route path="/dashboard" element={<DashboardLayout />}>
          <Route path="electricity" element={<ElectricityDashboardPage />} />
          <Route path="carbon-footprint" element={<CarbonFootprintPage />} />
        </Route>
        <Route path="/" element={<Navigate to="/login" />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

## 实现步骤（根据优先级调整）

### Phase 1: 环境搭建 (1-2小时) - 🔴 P0

**任务清单**:
- [ ] 初始化React项目 (`npx create-react-app frontend` 或 `npm create vite@latest`)
- [ ] 初始化Node.js后端项目 (`npm init -y`)
- [ ] 安装后端依赖: express, mssql, bcrypt, jsonwebtoken, cors, dotenv
- [ ] 安装前端依赖: react-router-dom, axios, chart.js, react-chartjs-2
- [ ] 配置SQL Server连接
- [ ] 创建基础项目结构（按照上述目录结构）
- [ ] 配置.env文件（数据库连接、JWT密钥等）
- [ ] 创建UIContext（UI类的React实现）

**参考**: CaseStudy Week 1-2 任务

---

### Phase 2: 数据库设计 (1小时) - 🔴 P0

**任务清单**:
- [ ] 根据ERD图创建数据库表
- [ ] 创建USERS表
- [ ] 创建CARBON_FOOTPRINT表（独立表，包含calculation_source字段）
- [ ] 创建BILL_ENTRY表
- [ ] 创建REPORTS表（Skeleton）
- [ ] 创建ELECTRICITY_REPORTS表（Skeleton）
- [ ] 创建HISTORY_LOGS表（Skeleton）
- [ ] 创建REPORT_HISTORY表（Skeleton）
- [ ] 插入测试数据（至少2个用户：1个Admin，1个TeamMember）
- [ ] 插入测试碳足迹数据和账单条目
- [ ] 验证表关系和外键约束

**参考**: instructions/ref/erd-diagram.puml

---

### Phase 3: 后端API开发 - P0核心功能 (4-5小时)

#### 3.1 User认证与管理模块 (2小时) - 🔴 P0
- [ ] 实现User类（models/User.js）
- [ ] 实现Admin类（继承User，包含addUser, editUserPermissions, assignRole方法）
- [ ] 实现TeamMember类（继承User）
- [ ] 实现AccessControl类
- [ ] 创建认证中间件（JWT验证）
- [ ] 实现POST /api/user/login - User.login()
- [ ] 实现POST /api/user/logout - User.logout()
- [ ] **实现POST /api/user/create - Admin.addUser()** （无注册功能）
- [ ] **实现PUT /api/user/:id - Admin.editUser()**
- [ ] **实现PUT /api/user/:id/permissions - Admin.editUserPermissions()**
- [ ] **实现GET /api/users - Admin获取用户列表**
- [ ] 测试登录流程和用户管理功能

#### 3.2 CarbonFootprint模块（两种模式） (2小时) - 🔴 P0
- [ ] 实现CarbonFootprint类（models/CarbonFootprint.js）
  - [ ] 支持数据库自动计算模式
  - [ ] 支持用户临时上传模式
- [ ] 实现BillEntry类（models/BillEntry.js）
- [ ] 实现GET /api/carbon-footprint/:userId - 获取用户碳足迹
- [ ] 实现POST /api/carbon-footprint/calculate-from-db - 从数据库计算
- [ ] 实现POST /api/carbon-footprint/calculate-from-upload - 临时上传计算（不持久化）
- [ ] 实现POST /api/carbon-footprint/bill-entry - addBillEntry()
- [ ] 实现GET /api/carbon-footprint/:userId/history - 获取历史数据
- [ ] 测试两种模式的碳足迹计算逻辑

#### 3.3 Skeleton类实现 (0.5-1小时) - 🟢 P2
- [ ] 实现Report基类（models/Report.js）- Skeleton
- [ ] 实现ElectricityReport类（继承Report）- Skeleton
- [ ] 实现Dashboard类（models/Dashboard.js）- Skeleton
- [ ] 实现Visualizer类（models/Visualizer.js）- Skeleton
- [ ] 实现Forecast类（models/Forecast.js）- Skeleton（标注未来实现）
- [ ] 实现PeriodComparison类（models/PeriodComparison.js）- Skeleton

**参考**: 
- class-diagram-optimized.puml
- CaseStudy Week 3-5 任务

---

### Phase 4: 前端开发 - P0核心功能 (5-6小时)

#### 4.1 基础设置 (1小时) - 🔴 P0
- [ ] 创建API工具类（utils/api.js）
- [ ] 配置Axios拦截器（添加JWT token）
- [ ] 创建UIContext（UI类实现）
- [ ] 创建所有Service类（对应类图）
- [ ] 配置React Router
- [ ] 创建基础布局组件（Header, Sidebar）

#### 4.2 登录页面 (1小时) - 🔴 P0
- [ ] 创建LoginPage组件
- [ ] 实现表单验证
- [ ] 调用UserService.login()
- [ ] 存储用户信息（包含所有User attributes）
- [ ] 实现角色基础的路由跳转（Admin → 用户管理，TeamMember → Dashboard）
- [ ] 测试登录流程

#### 4.3 用户管理页面（Admin专用） (1.5小时) - 🔴 P0
- [ ] 创建UserManagementPage组件（Admin专用）
- [ ] 显示用户列表
- [ ] 实现添加用户功能（Admin.addUser()）
- [ ] 实现编辑用户功能（Admin.editUser()）
- [ ] 实现分配权限功能（Admin.editUserPermissions()）
- [ ] 实现角色分配（Admin.assignRole()）
- [ ] 权限检查（只有Admin可访问）
- [ ] 测试用户管理流程

#### 4.4 碳足迹页面（两种模式） (2小时) - 🔴 P0
- [ ] 创建CarbonFootprintPage组件
- [ ] **模式1: 数据库自动计算**
  - [ ] 显示从数据库计算的碳足迹数据
  - [ ] 显示历史碳足迹趋势图表
  - [ ] 显示BillEntry列表
- [ ] **模式2: 用户临时上传**
  - [ ] 创建上传表单（年、月、用电量）
  - [ ] 实时计算碳足迹（不持久化）
  - [ ] 显示计算结果
  - [ ] 添加提示：数据不会保存
- [ ] 模式切换功能（Tab或按钮）
- [ ] 使用Chart.js可视化数据
- [ ] 测试两种模式的碳足迹计算

#### 4.5 Skeleton页面（框架搭建） (0.5-1小时) - 🟢 P2
- [ ] 创建ElectricityDashboardPage组件（Skeleton）
- [ ] 创建WaterDashboardPage组件（Skeleton）
- [ ] 创建ThermalDashboardPage组件（Skeleton）
- [ ] 添加导航链接（暂时显示"Coming Soon"）

**参考**: 
- Figma设计: instructions/ref/figma/
- CaseStudy Week 7-9 任务

---

### Phase 5: P1功能（时间允许） - 电力仪表板基础版 (3-4小时)

#### 5.1 ElectricityReport后端 (1.5小时) - 🟡 P1
- [ ] 完善ElectricityReport类实现
- [ ] 实现GET /api/electricity/report/:userId
- [ ] 实现POST /api/electricity/report/calculate-carbon
- [ ] 实现GET /api/electricity/report/carbon-chart/:userId
- [ ] 确保ElectricityReport关联CarbonFootprint数据

#### 5.2 电力仪表板前端 (2小时) - 🟡 P1
- [ ] 完善ElectricityDashboardPage组件
- [ ] 显示电力消耗和发电数据
- [ ] **集成显示碳足迹数据**
- [ ] 实现基础图表可视化
- [ ] 计算自给自足率
- [ ] 测试完整流程

---

### Phase 6: 集成与测试 (2-3小时) - 🔴 P0

#### 6.1 前后端联调 (1小时)
- [ ] 测试登录流程（Admin和TeamMember）
- [ ] 测试用户管理流程（Admin添加/编辑用户）
- [ ] 测试碳足迹计算流程（两种模式）
- [ ] 测试权限控制（Admin vs TeamMember）
- [ ] 修复API调用问题

#### 6.2 功能测试 (1小时)
- [ ] 测试User.login()和logout()
- [ ] 测试Admin.addUser()和editUserPermissions()
- [ ] 测试CarbonFootprint.calculateFootprint()（两种模式）
- [ ] 测试CarbonFootprint.addBillEntry()
- [ ] 测试AccessControl.checkPermission()
- [ ] 测试数据持久化（模式1）和临时数据（模式2）

#### 6.3 UI/UX优化 (1小时)
- [ ] 添加加载状态（使用UIContext）
- [ ] 添加错误处理（使用UI.displayWarning()）
- [ ] 优化图表显示
- [ ] 响应式设计调整
- [ ] 添加用户反馈提示

**参考**: CaseStudy Week 11-12 任务

---

## 类图关系检查清单

### 必须实现的继承关系
- [ ] `Admin --|> User` : Admin继承User
- [ ] `TeamMember --|> User` : TeamMember继承User
- [ ] `ElectricityReport --|> Report` : ElectricityReport继承Report

### 必须实现的组合关系（强拥有）
- [ ] `UI "1" *-- "*" Dashboard` : UI组合Dashboard
- [ ] `UI "1" *-- "1" Filter` : UI组合Filter
- [ ] `UI "1" *-- "1" ReportCustomization` : UI组合ReportCustomization
- [ ] `UI "1" *-- "1" CarbonFootprint` : UI组合CarbonFootprint
- [ ] `CarbonFootprint "1" o-- "*" BillEntry` : CarbonFootprint包含BillEntry

### 必须实现的聚合关系（弱拥有）
- [ ] `UI "1" o-- "*" Report` : UI聚合Report
- [ ] `User "1" o-- "*" HistoryLog` : User生成HistoryLog

### 必须实现的依赖关系
- [ ] `CarbonFootprint "1" --> "1" ElectricityReport` : CarbonFootprint计算ElectricityReport
- [ ] `Dashboard --> Visualizer` : Dashboard使用Visualizer
- [ ] `Dashboard --> Report` : Dashboard聚合Report
- [ ] `Dashboard ..> AccessControl` : Dashboard检查权限
- [ ] `Visualizer --> Report` : Visualizer可视化Report
- [ ] `Forecast --> Report` : Forecast生成Report
- [ ] `Report --> PeriodComparison` : Report比较PeriodComparison
- [ ] `Report ..> Database` : Report查询Database
- [ ] `Report ..> AccessControl` : Report检查权限
- [ ] `User --> UI` : User使用UI
- [ ] `Admin ..> AccessControl` : Admin使用AccessControl

### 关键集成点验证
- [ ] 电力报告中显示碳足迹数据
- [ ] ElectricityReport调用CarbonFootprint.calculateFootprint()
- [ ] Dashboard调用AccessControl.checkPermission()
- [ ] Report调用Database.executeQuery()
- [ ] Visualizer生成图表数据

---

## 参考文档

### Phase 3 要求文档
- **主文档**: `instructions/Phase3_extracted.md`
  - 原型要求: Login + Carbon Footprint (non-trivial) + Electricity Dashboard
  - 提交要求: 源代码 + 演示

### Phase 2 设计文档
- **主文档**: `instructions/ref/Phase2_extracted.md`
  - Use Case描述 (Page 11-22)
  - 功能需求和非功能需求 (Page 5-6)
  - 数据存储和持久化 (Page 8-9)
  - UI设计 (Page 24-36)

### 案例研究和项目管理
- **主文档**: `instructions/ref/CaseStudy_extracted.md`
  - 工作分解结构 (WBS)
  - 任务依赖关系
  - 时间估算
  - 里程碑定义

### UML图表

#### 类图（最重要 - 必须严格遵循）
- **文件**: `instructions/ref/class-diagram-optimized.puml`
- **用途**: 所有类的attributes和methods定义
- **关键包**: User Management, Reporting System, Carbon Analysis, User Interface, Visualization, Analytics

#### ERD图
- **文件**: `instructions/ref/erd-diagram.puml`
- **用途**: 数据库表结构设计
- **关键表**: USERS, REPORTS, ELECTRICITY_REPORTS, CARBON_FOOTPRINT, BILL_ENTRY

#### 部署图
- **文件**: `instructions/ref/deployment-diagram.puml`
- **用途**: 系统架构和技术栈
- **架构**: 三层架构（Presentation, Application, Data Layer）

#### 用例图
- **文件**: `instructions/ref/usecase-diagram.puml`
- **用途**: 系统功能概览
- **关键用例**: Login, View Dashboard, Carbon Footprint Calculation

#### 序列图（新完成）
- **电力仪表板**: `deliverables/seq-electricity-dashboard.puml`
  - 实时模式 vs 时间段选择模式
  - 预测和期间对比流程
  - 报告导出流程
- **AI聊天机器人**: `deliverables/seq-ai-chatbot.puml`
  - 自然语言查询处理
  - 权限检查流程
  - 错误处理流程
- **测验管理**: `deliverables/seq-quiz-management.puml`
  - 管理员创建测验
  - QR码生成
  - 结果查看

#### 活动图（新完成）
- **电力仪表板**: `deliverables/activity-electricity-dashboard.puml`
  - 用户交互流程
  - 数据加载和可视化
  - 报告导出决策点
- **AI聊天机器人**: `deliverables/activity-ai-chatbot.puml`
  - 查询处理流程
  - 权限验证
  - 错误处理分支

### UI设计
- **Figma导出**: `instructions/ref/figma/` 文件夹
  - 管理员界面设计
  - 团队成员界面设计
  - 报告导出界面: export report 1-4.png
  - 仪表板布局参考

---

## 最小可行产品 (MVP) 功能清单（更新版）

### 🔴 P0 (必须实现 - Prototype演示核心)

#### 登录与用户管理模块
- [ ] User类及其login/logout方法
- [ ] Admin类（继承User）及其用户管理方法
  - [ ] addUser() - 添加新用户
  - [ ] editUserPermissions() - 编辑权限
  - [ ] assignRole() - 分配角色
- [ ] TeamMember类（继承User）及其getPermissions()方法
- [ ] AccessControl权限检查
- [ ] JWT token生成和验证
- [ ] 登录页面UI
- [ ] **用户管理页面UI（Admin专用）**
  - [ ] 用户列表展示
  - [ ] 添加用户表单
  - [ ] 编辑用户表单
  - [ ] 权限分配界面
- [ ] 角色基础路由（Admin → 用户管理，TeamMember → Dashboard）
- [ ] **重要**: 无注册功能，所有用户由Admin创建

#### 碳足迹模块（两种模式）
- [ ] CarbonFootprint类及其方法
  - [ ] calculateFootprint() - 计算碳足迹
  - [ ] addBillEntry() - 添加账单条目
  - [ ] calculateFromDatabase() - 模式1：数据库自动计算
  - [ ] calculateFromUserUpload() - 模式2：用户临时上传
  - [ ] overrideWithDIY() - DIY数据覆盖
- [ ] BillEntry类
- [ ] 碳足迹页面UI
  - [ ] **模式1**: 数据库自动计算展示
    - [ ] 显示历史碳足迹数据
    - [ ] 显示账单条目列表
    - [ ] 数据持久化
  - [ ] **模式2**: 用户临时上传
    - [ ] 上传表单（年、月、用电量）
    - [ ] 实时计算显示
    - [ ] 数据不持久化提示
  - [ ] 模式切换功能
  - [ ] 基础图表可视化
- [ ] 独立的CARBON_FOOTPRINT数据库表

#### UI类实现
- [ ] UIContext（使用React Context API）
- [ ] UI类的attributes和methods
  - [ ] currentView
  - [ ] renderView()
  - [ ] updateUI()
  - [ ] displayWarning()
- [ ] 组合关系实现
  - [ ] UI owns Dashboard
  - [ ] UI owns Filter
  - [ ] UI owns ReportCustomization
  - [ ] UI owns CarbonFootprint

#### 类图关系（核心）
- [ ] 所有继承关系实现（Admin/TeamMember → User）
- [ ] UI类的组合关系
- [ ] AccessControl的依赖关系

---

### 🟡 P1 (时间允许则实现 - 增强演示效果)

#### 电力仪表板模块（基础版）
- [ ] ElectricityReport类及其calculateCarbonFootprint方法
- [ ] Report基类（Skeleton）
- [ ] Dashboard类及其generateView方法
- [ ] 电力仪表板页面UI
  - [ ] 电力消耗数据展示（基础图表）
  - [ ] **电力报告中集成碳足迹数据显示**
  - [ ] 自给自足率计算
  - [ ] 基础的数据可视化
- [ ] CarbonFootprint和ElectricityReport的依赖关系
- [ ] Dashboard和Visualizer的使用关系

#### Visualizer类（基础功能）
- [ ] Visualizer类及其基础图表生成方法
- [ ] generateGraph() - 生成图表
- [ ] calculateSelfSufficiency() - 计算自给自足率

---

### 🟢 P2 (Skeleton实现 - 框架搭建，功能未来实现)

#### Skeleton类
- [ ] Report基类（完整skeleton）
- [ ] WaterReport类（skeleton）
- [ ] ThermalReport类（skeleton）
- [ ] Filter类（skeleton）
- [ ] ReportCustomization类（skeleton）
- [ ] Forecast类（skeleton，标注需要真实预测算法）
- [ ] PeriodComparison类（skeleton）

#### Skeleton页面
- [ ] WaterDashboard页面（Coming Soon）
- [ ] ThermalDashboard页面（Coming Soon）
- [ ] 3D可视化页面（Coming Soon）

#### 未来功能（标注但不实现）
- [ ] Forecast预测功能（需要真实算法，如ARIMA、Prophet等）
- [ ] Period Comparison（YoY/PoP）
- [ ] 报告导出（PDF）
- [ ] 高级图表交互
- [ ] 数据筛选和搜索
- [ ] 响应式设计优化

---

## 开发注意事项

### 1. 类图严格遵循
- **所有类必须包含类图中定义的所有attributes**
- **所有方法必须按照类图中的签名实现**
- **类之间的关系必须在代码中体现**
- **方法命名必须与类图保持一致**

### 2. 碳足迹集成
- ElectricityReport必须调用CarbonFootprint.calculateFootprint()
- 电力报告页面必须同时显示电力数据和碳足迹数据
- 两者的关联必须清晰可见

### 3. 数据关联
- 电力数据和碳足迹数据通过用户ID关联
- 计算公式：碳排放 = 电力消耗 × 碳排放因子（0.5 kg CO2/kWh）
- 确保数据一致性

### 4. 权限控制
- 所有API调用前必须检查权限
- 使用AccessControl.checkPermission()
- Admin有全部权限，TeamMember根据permissions列表

### 5. 错误处理
- 实现基础的错误处理和用户提示
- API调用失败时显示友好错误信息
- 数据验证和边界检查

### 6. 代码复用
- 尽量复用组件和API逻辑
- 创建可重用的图表组件
- 统一的API调用封装

### 7. 测试数据
- 准备充足的测试数据以展示功能
- 至少2个用户（Admin和TeamMember）
- 至少3个月的电力和碳足迹数据

### 8. 性能考虑
- 对于原型，性能不是首要考虑
- 但要避免明显的性能问题
- 合理使用loading状态

### 9. 文档和注释
- 在代码中添加必要的注释
- 说明类图对应关系
- 标注关键业务逻辑

---

## 预计工作量（更新版）

### 总体估算（按优先级）
- **P0 - 必须完成**:
  - 环境搭建: 1-2小时
  - 数据库设计: 1小时
  - 后端API（登录+用户管理+碳足迹）: 4-5小时
  - 前端开发（登录+用户管理+碳足迹）: 5-6小时
  - 集成测试: 2-3小时
  - **P0小计**: 约13-17小时

- **P1 - 时间允许**:
  - 电力仪表板后端: 1.5小时
  - 电力仪表板前端: 2小时
  - **P1小计**: 约3.5小时

- **P2 - Skeleton**:
  - Skeleton类和页面: 1-2小时
  - **P2小计**: 约1-2小时

- **总计**: 约17-22小时（建议预留2-3小时缓冲）

### 按模块估算（详细）

| 模块 | 后端 | 前端 | 测试 | 总计 | 优先级 |
|------|------|------|------|------|--------|
| 环境搭建 | 0.5h | 0.5h | 0.5h | 1.5h | 🔴 P0 |
| 数据库设计 | 1h | - | 0.5h | 1.5h | 🔴 P0 |
| 登录 | 1h | 1h | 0.5h | 2.5h | 🔴 P0 |
| 用户管理 | 1h | 1.5h | 0.5h | 3h | 🔴 P0 |
| 碳足迹（两种模式） | 2h | 2h | 1h | 5h | 🔴 P0 |
| UI类实现 | - | 1h | 0.5h | 1.5h | 🔴 P0 |
| 电力仪表板 | 1.5h | 2h | 1h | 4.5h | 🟡 P1 |
| Skeleton | 0.5h | 1h | 0.5h | 2h | 🟢 P2 |

### 按周计划

**第一周**（P0核心功能）:
- Day 1-2: 环境搭建 + 数据库设计 + 登录功能
- Day 3-4: 用户管理功能
- Day 5-7: 碳足迹功能（两种模式）

**第二周**（P1和测试）:
- Day 1-2: 电力仪表板（如果时间允许）
- Day 3-4: 集成测试和bug修复
- Day 5-7: UI优化 + 演示准备

**缓冲时间**: 预留2-3小时处理意外问题

---

## 演示准备

### 演示脚本（更新版）

#### 1. 登录演示 (1.5分钟)
- 展示登录页面
- **先使用Admin账号登录**
- 展示Admin登录后跳转到用户管理页面
- 说明User类的login()方法实现
- 说明系统为内部系统，无注册功能

#### 2. 用户管理演示 (2.5分钟) - 🔴 核心演示
- 展示用户列表
- **演示添加新用户**（Admin.addUser()）
  - 填写用户信息（firstName, lastName, email, password）
  - 选择角色（Admin或TeamMember）
  - 创建成功
- **演示编辑用户权限**（Admin.editUserPermissions()）
  - 选择TeamMember用户
  - 分配权限（如Electricity, Water权限）
  - 保存成功
- 说明Admin类的实现和继承关系
- 强调AccessControl.checkPermission()的使用

#### 3. 碳足迹追踪演示 (4分钟) - 🔴 核心演示（非平凡用例）
- 登出Admin，使用TeamMember账号登录
- 导航到碳足迹页面
- **模式1: 数据库自动计算**
  - 展示从数据库读取的碳足迹数据
  - 展示历史账单条目列表
  - 展示碳足迹趋势图表
  - 说明数据持久化存储
- **模式2: 用户临时上传**
  - 切换到上传模式
  - 演示上传电费账单（年、月、用电量）
  - 实时计算并显示碳足迹
  - **强调**: 数据不会保存，关闭浏览器后清除
  - 说明overrideWithDIY()方法
- 说明CarbonFootprint类的两种计算模式实现
- 说明独立表设计的优势

#### 4. 电力仪表板演示 (2分钟) - 🟡 如果时间允许
- 导航到电力仪表板
- 展示电力消耗和发电数据
- **重点展示**: 碳足迹数据集成显示
- 说明ElectricityReport.calculateCarbonFootprint()的调用
- 展示基础图表可视化
- 展示自给自足率计算
- 说明Dashboard.generateView()的使用

#### 5. UI类和类图对应关系说明 (2分钟)
- 展示类图文件（class-diagram-optimized.puml）
- **说明UI类的实现**（使用Context API）
  - 展示UIContext代码
  - 说明attributes和methods
  - 说明组合关系（owns Dashboard, Filter, CarbonFootprint等）
- 说明实现的核心类和方法
  - User抽象类及其子类（Admin, TeamMember）
  - CarbonFootprint类及其两种模式
  - AccessControl类
- 强调严格遵循类图的原则
- 展示关键关系的代码实现（继承、组合、依赖）

#### 6. Skeleton展示 (1分钟) - 🟢 可选
- 展示其他Dashboard页面（Water, Thermal）的skeleton
- 说明Forecast类标注为未来实现（需要真实预测算法）
- 说明框架已搭建，功能可扩展

### 演示数据准备
- 创建2个测试用户（admin@sait.ca, john.doe@sait.ca）
- 准备3-6个月的电力数据
- 准备对应的碳足迹数据
- 确保数据关联正确

### 技术问题准备
- 如何实现类图中的继承关系？
- 如何确保碳足迹数据显示在电力报告中？
- 如何实现权限控制？
- 数据库设计如何对应ERD图？
- 前后端如何通信？

---

## 下一步行动（更新版）

### 立即开始（按优先级）
1. ✅ 阅读完整的实现计划文档
2. ⬜ 确认开发环境（Node.js, SQL Server, React）
3. ⬜ 创建项目基础结构
4. ⬜ 设计并创建数据库表（重点：CARBON_FOOTPRINT独立表）
5. ⬜ 实现UIContext（UI类的React实现）
6. ⬜ 开始后端User类和认证API开发
7. ⬜ 开始Admin用户管理功能开发

### 开发顺序（按优先级）
**Week 1 - P0核心功能**:
- Day 1-2: 环境搭建 + 数据库 + UIContext + 登录模块
- Day 3-4: 用户管理模块（Admin添加/编辑用户）
- Day 5-7: 碳足迹模块（两种模式）

**Week 2 - P1和测试**:
- Day 1-2: 电力仪表板（如果时间允许）
- Day 3-4: 集成测试和bug修复
- Day 5-7: UI优化 + 演示准备

### 关键决策确认
- ✅ 碳足迹使用独立表设计（CARBON_FOOTPRINT表）
- ✅ UI类使用Context API实现
- ✅ Forecast功能标记为未来实现（需要真实算法）
- ✅ 优先级：Login + 用户管理 + 碳足迹 > 电力仪表板 > Skeleton
- ✅ 系统为内部系统，无注册功能

---

## 成功标准（更新版）

### 功能完整性 - P0（必须达成）
- [ ] **登录与用户管理**
  - [ ] Admin和TeamMember可以成功登录和登出
  - [ ] Admin可以添加新用户（无注册功能）
  - [ ] Admin可以编辑用户信息和权限
  - [ ] 角色基础路由正确（Admin → 用户管理，TeamMember → Dashboard）
  
- [ ] **碳足迹追踪（两种模式）**
  - [ ] 模式1：从数据库自动计算碳足迹
  - [ ] 模式2：用户临时上传计算（不持久化）
  - [ ] 两种模式可以切换
  - [ ] 数据可视化（图表）
  - [ ] 历史数据追踪
  
- [ ] **UI类实现**
  - [ ] UIContext正确实现
  - [ ] UI类的attributes和methods完整
  - [ ] 组合关系正确（owns Dashboard, Filter, CarbonFootprint等）
  
- [ ] **类图遵循**
  - [ ] 所有P0核心类都已实现（User, Admin, TeamMember, CarbonFootprint, BillEntry, AccessControl, UI）
  - [ ] 所有P0关键方法都已实现
  - [ ] 继承关系正确（Admin/TeamMember → User）

### 功能完整性 - P1（时间允许）
- [ ] 电力仪表板基础功能
- [ ] 电力报告中显示碳足迹数据
- [ ] 自给自足率计算

### 代码质量
- [ ] 代码结构清晰，遵循类图设计
- [ ] 所有类包含类图定义的attributes和methods
- [ ] 类之间的关系正确实现（继承、组合、依赖）
- [ ] 有适当的错误处理（使用UI.displayWarning()）
- [ ] 有必要的代码注释（标注类图对应关系）
- [ ] 数据库设计符合规范（独立表，外键约束）

### 演示就绪
- [ ] 系统可以稳定运行
- [ ] 有充足的测试数据（至少2个用户：1个Admin，1个TeamMember）
- [ ] 有测试碳足迹数据（至少3个月）
- [ ] UI界面友好，功能可用
- [ ] 可以清晰展示类图对应关系
- [ ] 可以演示两种碳足迹计算模式
- [ ] 可以演示用户管理功能
- [ ] 可以回答技术问题

### 演示重点
- [ ] **非平凡用例**: 碳足迹追踪（两种模式）
- [ ] **内部系统**: 无注册功能，Admin管理用户
- [ ] **类图遵循**: 严格按照类图实现
- [ ] **UI类实现**: 使用Context API实现UI类

---

**最后更新**: 2025-11-27  
**文档版本**: 3.0 (根据团队反馈更新)  
**状态**: 准备开始实现  

**重要提醒**: 
- 所有实现必须严格遵循 `class-diagram-optimized.puml`
- **Prototype优先级**: Login + 用户管理 + 碳足迹（两种模式）
- 系统为**内部系统**，无注册功能，用户由Admin统一管理
- 碳足迹支持**两种模式**：数据库自动计算 + 用户临时上传
- UI类使用**Context API**实现，符合类图要求
- Forecast功能需要**真实预测算法**，Prototype阶段不实现
- 这是Phase 3的核心要求，必须在演示中清晰展示

---

## 附录：快速参考

### 关键文件路径
```
instructions/
├── Phase3_extracted.md                    # Phase 3要求
├── ref/
│   ├── Phase2_extracted.md                # Phase 2设计文档
│   ├── CaseStudy_extracted.md             # 项目管理文档
│   ├── class-diagram-optimized.puml       # 类图（最重要）
│   ├── erd-diagram.puml                   # ERD图
│   ├── deployment-diagram.puml            # 部署图
│   ├── usecase-diagram.puml               # 用例图
│   └── figma/                             # UI设计

deliverables/
├── seq-electricity-dashboard.puml         # 电力仪表板序列图
├── seq-ai-chatbot.puml                    # AI聊天机器人序列图
├── seq-quiz-management.puml               # 测验管理序列图
├── activity-electricity-dashboard.puml    # 电力仪表板活动图
└── activity-ai-chatbot.puml               # AI聊天机器人活动图
```

### 核心类快速索引
- **User** (抽象): id, firstName, lastName, email, password, role | createUser(), login(), logout()
- **CarbonFootprint**: id, co2Emission, billEntries | calculateFootprint(), addBillEntry(), overrideWithDIY()
- **BillEntry**: id, year, month, electricityUsage
- **ElectricityReport** (继承Report): electricityConsumption, electricityGeneration | calculateCarbonFootprint(), generateHotspotMap(), generateCarbonVsConsumptionChart()
- **Dashboard**: id, dashboardType | generateView(), filterByPermissions()
- **Visualizer**: id, visualizeType | generateGraph(), calculateSelfSufficiency(), renderHotspotMap()

### 关键关系快速索引
- CarbonFootprint "1" --> "1" ElectricityReport : calculates
- UI "1" *-- "1" CarbonFootprint : owns
- ElectricityReport --|> Report : is-a
- Dashboard --> Visualizer : uses
- Dashboard ..> AccessControl : checks permission

---

**准备好了吗？让我们开始构建EcoSphere原型！** 🚀
