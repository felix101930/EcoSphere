# EcoSphere 系统架构文档

**项目**: EcoSphere - Smart Building Analytics System  
**最后更新**: 2025-11-28  
**版本**: 1.0  
**状态**: ✅ P0已完成 | 🔄 P1进行中

---

## 📋 目录

1. [架构概览](#1-架构概览)
2. [技术栈](#2-技术栈)
3. [项目结构](#3-项目结构)
4. [类设计与实现](#4-类设计与实现)
5. [API架构](#5-api架构)
6. [组件架构](#6-组件架构)
7. [前后端关系](#7-前后端关系)
8. [设计理念](#8-设计理念)
9. [实现进度](#9-实现进度)

---

## 1. 架构概览

### 1.1 系统架构

EcoSphere采用**三层架构**设计：

```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer (前端)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   UI组件     │  │  Service层   │  │  Context API │     │
│  │  (React)     │  │  (API调用)   │  │  (状态管理)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                          ↓ HTTP/JSON ↓
┌─────────────────────────────────────────────────────────────┐
│                   Application Layer (后端)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Routes     │→ │ Controllers  │→ │  Services    │     │
│  │  (API端点)   │  │  (请求处理)  │  │  (业务逻辑)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                          ↓ 数据操作 ↓
┌─────────────────────────────────────────────────────────────┐
│                      Data Layer (数据层)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Mock数据    │  │  外部API     │  │  未来:SQL    │     │
│  │  (JSON文件)  │  │(Electricity  │  │   Server     │     │
│  │              │  │    Maps)     │  │              │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 核心模块

| 模块 | 状态 | 描述 |
|------|------|------|
| 登录模块 | ✅ 完成 | 用户认证和会话管理 |
| 用户管理模块 | ✅ 完成 | Admin管理用户和权限 |
| 碳足迹追踪模块 | ✅ 完成 | 两种计算模式 + 可视化 |
| 电力仪表板模块 | ⏳ 待开始 | 电力数据展示 |

### 1.3 数据流

```
用户操作 → UI组件 → Service层 → HTTP请求 → 后端API
                                              ↓
                                          Controller
                                              ↓
                                          Service
                                              ↓
                                          数据源
                                              ↓
                                          JSON响应
                                              ↓
前端展示 ← UI组件 ← Service层 ← HTTP响应 ← 后端API
```

---

## 2. 技术栈

### 2.1 前端技术栈 ✅

| 技术 | 版本 | 用途 | 选择理由 |
|------|------|------|----------|
| **Vite** | Latest | 构建工具 | 启动快、热更新快 |
| **React** | 18.x | UI框架 | 组件化、生态完善 |
| **Material-UI** | 5.x | UI组件库 | 专业、组件丰富 |
| **Chart.js** | 4.x | 图表库 | 免费、本地生成 |
| **React Router** | 6.x | 路由管理 | 标准路由方案 |
| **Context API** | React内置 | 状态管理 | 轻量级、适合项目规模 |

**为什么选择这些技术**：
- **Vite**: 比Create React App更快，开发体验更好
- **Material-UI**: 提供专业的UI组件，符合SAIT设计规范
- **Chart.js**: 免费、无需外部API、功能强大
- **Context API**: 项目规模适中，不需要Redux的复杂性


### 2.2 后端技术栈 ✅

| 技术 | 版本 | 用途 | 选择理由 |
|------|------|------|----------|
| **Node.js** | 18.x+ | 运行时环境 | JavaScript全栈 |
| **Express.js** | 4.x | Web框架 | 简单、灵活 |
| **RESTful API** | - | API设计 | 标准、易于理解 |

**架构模式**: Routes → Controllers → Services → Utils

### 2.3 数据存储（Prototype阶段）⚠️

| 方案 | 状态 | 说明 |
|------|------|------|
| **Mock数据** | ✅ 当前使用 | JSON文件存储 |
| **SQL Server** | 🔄 未来迁移 | Week 1-2计划 |

**Mock数据文件**:
- `mock-data/users.json` - 用户数据
- `mock-data/electricity.json` - 电力数据（3个月，hourly）
- `mock-data/carbonFootprint.json` - 碳足迹历史数据

**为什么使用Mock数据**：
- 客户数据表结构未最终确定
- Prototype阶段快速开发
- 便于演示和测试

### 2.4 外部API ✅

| API | 用途 | 状态 |
|-----|------|------|
| **Electricity Maps API** | 获取实时碳强度（Alberta, Calgary） | ✅ 已集成 |

---

## 3. 项目结构

### 3.1 前端结构

```
ecosphere-frontend/
├── src/
│   ├── assets/                 # 静态资源
│   │   ├── loginbackground.jpg
│   │   ├── sait-logo_vert.svg
│   │   └── sait-logo_horz.svg
│   │
│   ├── components/             # React组件
│   │   ├── Common/             # 通用组件
│   │   │   ├── AlertMessage.jsx
│   │   │   ├── ErrorBoundary.jsx
│   │   │   └── LoadingSpinner.jsx
│   │   ├── Layout/             # 布局组件
│   │   │   ├── Sidebar.jsx
│   │   │   └── AIChatbot.jsx
│   │   ├── UserManagement/     # 用户管理组件
│   │   │   ├── UserTable.jsx
│   │   │   ├── UserTableRow.jsx
│   │   │   ├── UserDialog.jsx
│   │   │   └── UserForm.jsx
│   │   └── CarbonFootprint/    # 碳足迹组件
│   │       └── CustomCalculator.jsx
│   │
│   ├── pages/                  # 页面组件（容器）
│   │   ├── LoginPage.jsx
│   │   ├── DashboardPage.jsx
│   │   ├── UserManagementPage.jsx
│   │   ├── CarbonFootprintPage.jsx
│   │   └── ComingSoonPage.jsx
│   │
│   ├── contexts/               # React Context
│   │   └── AuthContext.jsx
│   │
│   ├── hooks/                  # 自定义Hooks
│   │   └── useAuth.js
│   │
│   ├── services/               # API服务
│   │   ├── UserService.js
│   │   ├── ElectricityService.js
│   │   └── ElectricityMapsService.js
│   │
│   ├── models/                 # 数据模型（工具类）
│   │   ├── User.js
│   │   ├── Admin.js
│   │   ├── TeamMember.js
│   │   ├── AccessControl.js
│   │   ├── CarbonFootprint.js
│   │   ├── BillEntry.js
│   │   ├── Report.js
│   │   └── ElectricityReport.js
│   │
│   ├── App.jsx                 # 主应用组件
│   ├── main.jsx                # 入口文件
│   └── index.css               # 全局样式
│
├── .env                        # 环境变量
├── package.json                # 依赖配置
└── vite.config.js              # Vite配置
```

### 3.2 后端结构

```
ecosphere-backend/
├── config/                     # 配置文件
│   └── config.js
│
├── routes/                     # API路由
│   ├── userRoutes.js
│   └── electricityRoutes.js
│
├── controllers/                # 控制器
│   ├── userController.js
│   └── electricityController.js
│
├── services/                   # 业务逻辑
│   ├── userService.js
│   └── electricityService.js
│
├── models/                     # 数据模型（完整类）
│   ├── User.js
│   ├── Admin.js
│   ├── TeamMember.js
│   ├── AccessControl.js
│   ├── CarbonFootprint.js
│   ├── BillEntry.js
│   ├── Report.js
│   └── ElectricityReport.js
│
├── utils/                      # 工具函数
│   └── fileHelper.js
│
├── server.js                   # 服务器入口
├── .env                        # 环境变量
└── package.json                # 依赖配置
```

### 3.3 为什么这样组织

**前端组织原则**：
1. **按功能分组**: components/按功能模块组织
2. **分离关注点**: pages/（容器）vs components/（展示）
3. **服务层独立**: services/封装所有API调用
4. **模型层轻量**: models/只包含工具类

**后端组织原则**：
1. **分层架构**: Routes → Controllers → Services → Models
2. **单一职责**: 每层只负责自己的事情
3. **易于测试**: 每层可以独立测试
4. **易于扩展**: 添加新功能只需添加新文件

---


## 4. 类设计与实现

### 4.1 类图遵循原则

所有类的设计严格遵循 `class-diagram-optimized.puml` 中的定义。

**设计原则**：
- ✅ 所有类必须包含类图中定义的所有attributes
- ✅ 所有方法必须按照类图中的签名实现
- ✅ 类之间的关系必须在代码中体现
- ✅ 命名必须与类图保持一致

### 4.2 已实现的类（P0 - 100%完成）

#### 4.2.1 User类（抽象类）✅

**Package**: User Management  
**前端位置**: `ecosphere-frontend/src/models/User.js`  
**后端位置**: `ecosphere-backend/models/User.js`

**Attributes**:
```javascript
- id: int
- firstName: String
- lastName: String
- email: String
- password: String
- role: String
```

**Methods**:
```javascript
+ createUser(id, firstName, lastName, email, password, role): void
+ login(): void
+ logout(): void
```

**关系**:
- 被继承: `Admin --|> User`, `TeamMember --|> User`

---

#### 4.2.2 Admin类（继承User）✅

**Package**: User Management  
**前端位置**: `ecosphere-frontend/src/models/Admin.js`  
**后端位置**: `ecosphere-backend/models/Admin.js`

**额外Methods**:
```javascript
+ addUser(userData): void
+ editUserPermissions(userId, permissions): void
+ assignRole(userId, role): void
+ createQuiz(): void
+ editQuiz(quizId): void
+ deleteQuiz(quizId): void
```

**关系**:
- 继承: `Admin --|> User`
- 依赖: `Admin ..> AccessControl : uses`

---

#### 4.2.3 TeamMember类（继承User）✅

**Package**: User Management  
**前端位置**: `ecosphere-frontend/src/models/TeamMember.js`  
**后端位置**: `ecosphere-backend/models/TeamMember.js`

**额外Attributes**:
```javascript
- promptChat: int
- permissions: List<String>
```

**额外Methods**:
```javascript
+ getPermissions(): List<String>
```

**关系**:
- 继承: `TeamMember --|> User`

---

#### 4.2.4 AccessControl类✅

**Package**: Security  
**前端位置**: `ecosphere-frontend/src/models/AccessControl.js`  
**后端位置**: `ecosphere-backend/models/AccessControl.js`

**Methods** (Static):
```javascript
+ checkPermission(user, module): boolean
+ updateUserPermissions(userId, permissions): void
```

**关系**:
- 被使用: `Admin ..> AccessControl : uses`
- 被依赖: `UI ..> AccessControl : checks permission`

---

#### 4.2.5 CarbonFootprint功能✅

**实现方式**: 通过Service层实现（功能完整）

**相关文件**:
- `ElectricityMapsService.js` - 碳强度API和计算
- `ElectricityService.js` - 电力数据管理
- `CarbonFootprintPage.jsx` - 主页面容器
- `CustomCalculator.jsx` - 用户临时计算组件

**对应的类图Methods**:
```javascript
+ calculateFootprint(electricityUsage, emissionFactor): double
+ addBillEntry(year, month, electricityUsage): void
+ overrideWithDIY(): DataSet
```

---

#### 4.2.6 BillEntry功能✅

**实现方式**: 在CustomCalculator组件中作为React state

**数据结构**:
```javascript
{
  id: int,
  year: string,
  month: string,
  usage: number
}
```

---

### 4.3 待实现的类（P1 - 待开始）

#### 4.3.1 Report类（抽象类）⏳

**Package**: Reporting System  
**计划位置**: `ecosphere-frontend/src/models/Report.js`

**Attributes**:
```javascript
- id: int
- reportType: String
```

**Methods**:
```javascript
+ exportReport(): void
+ filterReport(filters): void
```

---

#### 4.3.2 ElectricityReport类（继承Report）⏳

**Package**: Reporting System  
**计划位置**: `ecosphere-frontend/src/models/ElectricityReport.js`

**额外Attributes**:
```javascript
- electricityConsumption: double
- electricityGeneration: double
```

**Methods**:
```javascript
+ calculateCarbonFootprint(emissionFactor): double
+ generateHotspotMap(data): Map
+ generateCarbonVsConsumptionChart(): Chart
```

---

#### 4.3.3 Dashboard类⏳

**Package**: User Interface  
**计划位置**: `ecosphere-frontend/src/models/Dashboard.js`

**Attributes**:
```javascript
- id: int
- dashboardType: String
```

**Methods**:
```javascript
+ generateView(user, type): DashboardView
+ filterByPermissions(user): List
```

---

#### 4.3.4 Visualizer类⏳

**Package**: Visualization  
**计划位置**: `ecosphere-frontend/src/models/Visualizer.js`

**Attributes**:
```javascript
- id: int
- visualizeType: String
```

**Methods**:
```javascript
+ generateGraph(data, graphType): void
+ calculateSelfSufficiency(generation, consumption): double
+ renderHotspotMap(data): Map
```

---

### 4.4 类之间的关系

#### 继承关系（Inheritance）
```
User (抽象)
  ├── Admin ✅
  └── TeamMember ✅

Report (抽象) ⏳
  ├── WaterReport ⏸️
  ├── ElectricityReport ⏳
  └── ThermalReport ⏸️
```

#### 组合关系（Composition）
```
UI *-- Dashboard (多个) ⏳
UI *-- Filter (1个) ⏸️
UI *-- ReportCustomization (1个) ⏸️
UI *-- CarbonFootprint (1个) ✅
CarbonFootprint o-- BillEntry (多个) ✅
```

#### 依赖关系（Dependency）
```
Admin ..> AccessControl ✅
UI ..> AccessControl ✅
Dashboard ..> AccessControl ⏳
Report ..> AccessControl ⏳
```

---

### 4.5 实现进度统计

| 优先级 | 总数 | 已完成 | 待实现 | 完成率 |
|--------|------|--------|--------|--------|
| P0 | 6 | 6 | 0 | **100%** ✅ |
| P1 | 5 | 0 | 5 | 0% |
| P2 | 7 | 0 | 7 | 0% |
| **总计** | **18** | **6** | **12** | **33%** |

---


## 5. API架构

### 5.1 前后端通信流程

#### 完整的API调用链

```
前端                          后端
────────────────────────────────────────────────────────────

1. UI Component              
   (UserManagementPage.jsx)
   ↓
2. Service Layer             
   (UserService.js)
   ↓ HTTP Request (fetch)
   
                             3. Route
                                (userRoutes.js)
                                ↓
                             4. Controller
                                (userController.js)
                                ↓
                             5. Service
                                (userService.js)
                                ↓
                             6. Data
                                (users.json)
                                ↓
   ← JSON Response ←         7. Response
```

#### 示例：用户登录流程

**前端代码**:
```javascript
// 1. UI组件 (LoginPage.jsx)
const handleLogin = async () => {
  const user = await UserService.authenticate(email, password);
  if (user) {
    navigate('/dashboard');
  }
};

// 2. Service层 (UserService.js)
async authenticate(email, password) {
  const response = await fetch('http://localhost:3001/api/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password })
  });
  return await response.json();
}
```

**后端代码**:
```javascript
// 3. Route层 (userRoutes.js)
router.post('/auth/login', UserController.login);

// 4. Controller层 (userController.js)
static async login(req, res) {
  const { email, password } = req.body;
  const user = await UserService.authenticate(email, password);
  
  if (user) {
    res.json(user);
  } else {
    res.status(401).json({ error: 'Invalid credentials' });
  }
}

// 5. Service层 (userService.js)
static async authenticate(email, password) {
  const data = await FileHelper.readJSON('users.json');
  const user = data.users.find(u => 
    u.email === email && u.password === password
  );
  return user;
}
```

---

### 5.2 API端点列表

#### 用户管理API ✅

| 方法 | 端点 | 描述 | 前端调用 |
|------|------|------|----------|
| GET | `/api/users` | 获取所有用户 | `UserService.getAllUsers()` |
| GET | `/api/users/:id` | 获取单个用户 | `UserService.getUserById(id)` |
| POST | `/api/users` | 创建新用户 | `UserService.addUser(data)` |
| PUT | `/api/users/:id` | 更新用户 | `UserService.updateUser(id, data)` |
| DELETE | `/api/users/:id` | 删除用户 | `UserService.deleteUser(id)` |
| POST | `/api/auth/login` | 用户登录 | `UserService.authenticate(email, pwd)` |

#### 电力数据API ✅

| 方法 | 端点 | 描述 | 前端调用 |
|------|------|------|----------|
| GET | `/api/electricity/realtime` | 获取实时数据 | `ElectricityService.getRealTimeData()` |
| GET | `/api/electricity/range?startDate=...&endDate=...` | 获取范围数据 | `ElectricityService.getDataByRange(start, end)` |
| GET | `/api/electricity/longterm` | 获取长期数据 | `ElectricityService.getLongTermData()` |
| GET | `/api/electricity/metadata` | 获取元数据 | `ElectricityService.getMetadata()` |

---

### 5.3 Service层设计

#### 为什么需要Service层

**前端Service层**（`src/services/`）:
- ✅ 封装所有API调用
- ✅ 统一错误处理
- ✅ 避免组件直接调用API
- ✅ 易于测试和维护

**后端Service层**（`services/`）:
- ✅ 封装业务逻辑
- ✅ 数据操作和验证
- ✅ 与Controller分离
- ✅ 可复用的业务逻辑

#### Service vs Model的区别

| 方面 | Service | Model |
|------|---------|-------|
| **职责** | 业务逻辑、数据操作 | 数据结构、验证规则 |
| **状态** | 无状态（静态方法） | 有状态（实例） |
| **依赖** | 可以调用Model | 不依赖Service |
| **示例** | `UserService.authenticate()` | `new User()` |

---


## 6. 组件架构

### 6.1 组件化原则

#### 为什么需要组件化

**避免God Mode（上帝模式）**:
- ❌ 一个组件包含所有逻辑（300+ 行代码）
- ❌ 难以维护和测试
- ❌ 无法复用
- ❌ 团队协作困难

**组件化的好处**:
- ✅ 单一职责，易于理解
- ✅ 可复用，减少重复代码
- ✅ 易于测试
- ✅ 团队可以并行开发
- ✅ 易于维护和扩展

#### 核心原则

1. **单一职责原则** - 每个组件只做一件事
2. **可复用性** - 组件可以在多个地方使用
3. **小而精** - 单个组件不超过150行代码
4. **清晰命名** - 组件名称清楚表达功能
5. **Container/Presentation模式** - 分离数据逻辑和UI展示

---

### 6.2 组件分类

#### Common组件（通用组件）✅

可以在整个应用中使用的基础组件。

**示例**:
- `AlertMessage` - 提示消息
- `ErrorBoundary` - 错误边界
- `LoadingSpinner` - 加载动画

**特点**:
- 高度可复用
- 无业务逻辑
- 通过props配置

---

#### Feature组件（功能组件）✅

特定功能模块的组件。

**示例**:
- `UserManagement/` - 用户管理相关
  - `UserTable` - 用户列表表格
  - `UserTableRow` - 表格行
  - `UserDialog` - 添加/编辑对话框
  - `UserForm` - 用户表单
- `CarbonFootprint/` - 碳足迹相关
  - `CustomCalculator` - 自定义计算器

**特点**:
- 针对特定功能
- 可能包含一些业务逻辑
- 在功能模块内复用

---

#### Layout组件（布局组件）✅

应用的布局结构组件。

**示例**:
- `Sidebar` - 侧边栏
- `AIChatbot` - AI助手

**特点**:
- 定义应用结构
- 在多个页面使用
- 通常包含导航逻辑

---

#### Page组件（页面组件）✅

顶层容器组件，对应路由。

**示例**:
- `LoginPage` - 登录页面
- `UserManagementPage` - 用户管理页面
- `CarbonFootprintPage` - 碳足迹页面
- `DashboardPage` - 仪表板页面

**特点**:
- 容器组件
- 负责数据获取
- 组合子组件
- 对应路由

---

### 6.3 组件化实例：UserManagement

#### 重构前（God Mode）❌

```javascript
// UserManagementPage.jsx - 300+ lines
const UserManagementPage = () => {
  // 10+ state variables
  // 10+ functions
  // 所有UI渲染逻辑
  return (
    <Container>
      {/* 300+ lines of JSX */}
    </Container>
  );
};
```

**问题**:
- 300+ 行代码在一个文件
- 难以理解和维护
- 无法复用任何部分
- 测试困难

---

#### 重构后（组件化）✅

```javascript
// UserManagementPage.jsx - 180 lines (容器组件)
const UserManagementPage = () => {
  // 数据管理和业务逻辑
  return (
    <Container>
      <AlertMessage {...alertProps} />
      <UserTable {...tableProps} />
      <UserDialog {...dialogProps} />
    </Container>
  );
};

// UserTable.jsx - 40 lines (展示组件)
const UserTable = ({ users, onEdit, onDelete }) => {
  return (
    <Table>
      {users.map(user => (
        <UserTableRow key={user.id} user={user} {...actions} />
      ))}
    </Table>
  );
};

// UserTableRow.jsx - 45 lines (展示组件)
const UserTableRow = ({ user, onEdit, onDelete }) => {
  return <TableRow>{/* 用户信息 */}</TableRow>;
};

// UserDialog.jsx - 30 lines (展示组件)
const UserDialog = ({ open, onClose, onSubmit }) => {
  return (
    <Dialog open={open}>
      <UserForm {...formProps} />
    </Dialog>
  );
};

// UserForm.jsx - 60 lines (展示组件)
const UserForm = ({ formData, onChange }) => {
  return <Box>{/* 表单字段 */}</Box>;
};
```

**效果对比**:

| 指标 | 重构前 | 重构后 |
|------|--------|--------|
| 单文件代码行数 | 300+ | 最大180 |
| 组件数量 | 1 | 6 |
| 可复用组件 | 0 | 5 |
| 可测试性 | 困难 | 容易 |
| 可维护性 | 低 | 高 |
| 团队协作 | 困难 | 容易 |

---


## 7. 前后端关系

### 7.1 为什么前后端都有User.js

这是一个常见的疑问：为什么前端和后端都有同名的类文件？

#### 前端的User.js（工具类）

**位置**: `ecosphere-frontend/src/models/User.js`

**职责**:
- ✅ 提供工具函数（UserUtils）
- ✅ 数据验证
- ✅ 格式化
- ✅ 轻量级，无状态

**示例**:
```javascript
// 前端User.js - 工具类
class UserUtils {
  static validateEmail(email) {
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
  }
  
  static formatName(firstName, lastName) {
    return `${firstName} ${lastName}`;
  }
}
```

---

#### 后端的User.js（完整类）

**位置**: `ecosphere-backend/models/User.js`

**职责**:
- ✅ 完整的类定义
- ✅ 业务逻辑
- ✅ 数据操作
- ✅ 符合类图设计

**示例**:
```javascript
// 后端User.js - 完整类
class User {
  constructor() {
    this.id = null;
    this.firstName = '';
    this.lastName = '';
    this.email = '';
    this.password = '';
    this.role = '';
  }

  createUser(id, firstName, lastName, email, password, role) {
    // 完整的业务逻辑
  }

  login() {
    // 登录逻辑
  }

  logout() {
    // 登出逻辑
  }
}
```

---

#### 职责分离

| 方面 | 前端User.js | 后端User.js |
|------|-------------|-------------|
| **类型** | 工具类（Utils） | 完整类（Model） |
| **职责** | 辅助函数 | 业务逻辑 |
| **状态** | 无状态 | 有状态 |
| **复杂度** | 简单 | 复杂 |
| **依赖** | 无 | 可能依赖其他类 |

**为什么这样设计**:
- ✅ 前端不需要完整的业务逻辑
- ✅ 前端只需要工具函数
- ✅ 后端负责核心业务逻辑
- ✅ 职责清晰，易于维护

---

### 7.2 完整数据流

```
┌─────────────────────────────────────────────────────────────┐
│                         前端 (Frontend)                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. UI Component (React)                                    │
│     └── UserManagementPage.jsx                              │
│         ↓ 调用                                               │
│  2. Service Layer (API调用)                                 │
│     └── UserService.js                                      │
│         ↓ HTTP Request (JSON)                               │
└─────────────────────────────────────────────────────────────┘
                          ↓
                    HTTP/JSON
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                         后端 (Backend)                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  3. Routes (API端点定义)                                     │
│     └── userRoutes.js                                       │
│         ↓ 路由到                                             │
│  4. Controller (请求处理)                                    │
│     └── userController.js                                   │
│         ↓ 调用                                               │
│  5. Service (业务逻辑)                                       │
│     └── userService.js                                      │
│         ↓ 使用                                               │
│  6. Model (数据模型)                                         │
│     └── User.js                                             │
│         ↓ 操作                                               │
│  7. Data Storage                                            │
│     └── users.json (Mock数据)                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

### 7.3 状态管理

#### 为什么使用Context API而不是Redux

**Context API的优势**:
- ✅ React内置，无需额外依赖
- ✅ 轻量级，适合中小型项目
- ✅ 学习曲线低
- ✅ 足够满足当前需求

**Redux的劣势**（对于本项目）:
- ❌ 需要额外依赖
- ❌ 配置复杂（store, reducers, actions）
- ❌ 代码量大
- ❌ 对于当前项目规模过于复杂

**当前使用的Context**:
- `AuthContext` - 认证状态管理
  - 当前用户信息
  - 登录/登出状态
  - 权限信息

---


## 8. 设计理念

### 8.1 为什么采用组件化架构

**问题**: 传统的单体组件（God Mode）

```javascript
// ❌ 不好的做法
const UserManagementPage = () => {
  // 300+ lines of code
  // - All state management
  // - All business logic
  // - All UI rendering
  // - All event handlers
};
```

**解决方案**: 组件化架构

```javascript
// ✅ 好的做法
const UserManagementPage = () => {
  // 只负责组合和数据管理
  return (
    <>
      <AlertMessage />
      <UserTable />
      <UserDialog />
    </>
  );
};
```

**好处**:
1. **易于理解** - 每个组件职责单一
2. **易于测试** - 可以独立测试每个组件
3. **易于复用** - 组件可以在多处使用
4. **易于维护** - 修改一个组件不影响其他
5. **团队协作** - 不同人可以开发不同组件

---

### 8.2 为什么使用Mock数据

**原因**:
1. **客户数据表结构未最终确定**
   - GBTAC的SQL Server表结构还在讨论中
   - 避免频繁修改数据库设计

2. **Prototype阶段快速开发**
   - 不需要配置数据库连接
   - 不需要处理数据库权限
   - 开发速度更快

3. **便于演示和测试**
   - 数据可控，便于演示
   - 可以快速重置数据
   - 不依赖外部数据库

**未来计划**:
- 🔄 Week 1-2迁移到SQL Server
- 🔄 实现真实的数据库连接
- 🔄 实现数据迁移脚本

---

### 8.3 为什么选择这些技术

#### Vite vs Create React App

| 方面 | Vite | CRA |
|------|------|-----|
| 启动速度 | ⚡ 极快（秒级） | 🐌 慢（分钟级） |
| 热更新 | ⚡ 即时 | 🐌 较慢 |
| 构建速度 | ⚡ 快 | 🐌 慢 |
| 配置 | 简单 | 复杂 |
| 现代性 | 最新 | 较旧 |

**选择Vite**: 开发体验更好，速度更快

---

#### Material-UI vs Bootstrap

| 方面 | Material-UI | Bootstrap |
|------|-------------|-----------|
| 组件丰富度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 定制性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| React集成 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 文档质量 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 专业性 | 高 | 中 |

**选择Material-UI**: 更专业，组件更丰富，更适合企业级应用

---

#### Chart.js vs Plotly.js

| 方面 | Chart.js | Plotly.js |
|------|----------|-----------|
| 文件大小 | 小 | 大 |
| 免费 | ✅ 完全免费 | ⚠️ 部分功能收费 |
| 本地生成 | ✅ 是 | ✅ 是 |
| 学习曲线 | 低 | 高 |
| 功能 | 足够 | 更强大 |

**选择Chart.js**: 免费、轻量、足够满足需求

---

#### Context API vs Redux

| 方面 | Context API | Redux |
|------|-------------|-------|
| 学习曲线 | 低 | 高 |
| 代码量 | 少 | 多 |
| 配置复杂度 | 低 | 高 |
| 适合项目规模 | 中小型 | 大型 |
| React集成 | 内置 | 需要额外库 |

**选择Context API**: 项目规模适中，不需要Redux的复杂性

---

### 8.4 设计决策记录

#### 决策 #1: 使用Mock数据代替SQL Server
**日期**: 2025-11-27  
**决策**: Prototype阶段使用Mock数据  
**理由**: 客户数据表结构未确定，快速开发  
**影响**: Week 1-2需要迁移到SQL Server

---

#### 决策 #2: 使用Session-based认证代替JWT
**日期**: 2025-11-27  
**决策**: 使用sessionStorage存储用户信息  
**理由**: Prototype阶段简化实现  
**影响**: 未来需要实现JWT token认证

---

#### 决策 #3: 采用组件化架构
**日期**: 2025-11-28  
**决策**: 拆分大组件为多个小组件  
**理由**: 避免God Mode，提高可维护性  
**影响**: 代码组织更清晰，易于维护

---

#### 决策 #4: 集成Electricity Maps API
**日期**: 2025-11-28  
**决策**: 使用外部API获取实时碳强度  
**理由**: 提供真实的碳强度数据  
**影响**: 需要管理API密钥

---


## 9. 实现进度

### 9.1 已完成（P0 - 100%）✅

#### 登录模块 ✅

**功能**:
- ✅ 用户登录界面（email/password）
- ✅ 身份验证和Session管理
- ✅ 角色基础路由（Admin → User Management, TeamMember → Dashboard）

**实现文件**:
- 前端: `LoginPage.jsx`, `AuthContext.jsx`, `useAuth.js`
- 后端: `userRoutes.js`, `userController.js`, `userService.js`

**测试账号**:
- Admin: `admin.admin@edu.sait.ca` / `abcd1234`

---

#### 用户管理模块 ✅

**功能**:
- ✅ 查看用户列表
- ✅ 添加新用户
- ✅ 编辑用户信息
- ✅ 删除用户（不能删除自己）
- ✅ 分配角色和权限
- ✅ 多选权限（Electricity, Water, Thermal, 3D Model, Carbon Footprint）
- ✅ Sidebar权限过滤

**实现文件**:
- 前端组件: `UserManagementPage.jsx`, `UserTable.jsx`, `UserTableRow.jsx`, `UserDialog.jsx`, `UserForm.jsx`
- 前端Service: `UserService.js`
- 后端API: `userRoutes.js`, `userController.js`, `userService.js`

**已实现的类**:
- ✅ User (抽象类)
- ✅ Admin (继承User)
- ✅ TeamMember (继承User)
- ✅ AccessControl

---

#### 碳足迹追踪模块 ✅

**功能**:
- ✅ **模式1: Mock数据自动计算**
  - ✅ 从electricity.json读取电力数据
  - ✅ 集成Electricity Maps API获取实时碳强度
  - ✅ 自动计算碳足迹（CO2 = 电力消耗 × 碳强度）
  - ✅ 三种视图：Real-time (今天), Daily (用户选择), Long-term (3个月)
  
- ✅ **模式2: 用户临时上传（Custom Calculator）**
  - ✅ 用户上传电费账单（年、月、用电量）
  - ✅ 实时计算碳足迹
  - ✅ 数据不持久化（仅存在React state）
  - ✅ 动态添加/删除账单条目
  - ✅ 数据验证（防止重复、防止未来日期）
  - ✅ 自动排序（按年月）
  
- ✅ **数据可视化**
  - ✅ Chart.js双线折线图
  - ✅ 双Y轴（左：kg CO2，右：kWh）
  - ✅ SAIT配色方案
  - ✅ 响应式设计

**实现文件**:
- 前端页面: `CarbonFootprintPage.jsx`
- 前端组件: `CustomCalculator.jsx`
- 前端Service: `ElectricityService.js`, `ElectricityMapsService.js`
- 后端API: `electricityRoutes.js`, `electricityController.js`, `electricityService.js`
- Mock数据: `electricity.json`, `carbonFootprint.json`

**已实现的功能**:
- ✅ CarbonFootprint功能（通过Service层）
- ✅ BillEntry功能（通过CustomCalculator组件）

---

### 9.2 进行中（P1 - 0%）⏳

#### 电力仪表板模块 ⏳

**计划功能**:
- ⏳ 显示电力消耗和发电数据
- ⏳ 集成碳足迹数据显示
- ⏳ 自给自足率计算
- ⏳ 数据筛选功能

**待实现的类**:
- ⏳ Report (抽象类)
- ⏳ ElectricityReport (继承Report)
- ⏳ Dashboard
- ⏳ Visualizer

**技术栈**:
- 继续使用Chart.js
- 继续使用Mock数据
- 集成已完成的碳足迹功能

---

### 9.3 计划中（P2 - Skeleton）⏸️

#### 其他Report模块 ⏸️

**计划**:
- ⏸️ WaterReport类（Skeleton）
- ⏸️ ThermalReport类（Skeleton）
- ⏸️ 占位页面

---

#### 高级功能 ⏸️

**计划**:
- ⏸️ Forecast类（Skeleton）- 预测功能
- ⏸️ PeriodComparison类（Skeleton）- 期间对比
- ⏸️ Filter类（Skeleton）- 数据筛选
- ⏸️ ReportCustomization类（Skeleton）- 报告定制
- ⏸️ 3D可视化（Skeleton）

---

### 9.4 未来改进（Week 1-15）

#### Week 1-2: 数据库迁移
- 🔄 连接SQL Server数据库
- 🔄 创建数据库表
- 🔄 实现数据迁移脚本
- 🔄 更新Service层

#### Week 3-5: 认证系统升级
- 🔄 实现JWT token认证
- 🔄 实现token刷新机制
- 🔄 实现密码加密（bcrypt）

#### Week 7-9: 完整Report模块
- 🔄 实现完整的ElectricityReport
- 🔄 实现WaterReport
- 🔄 实现ThermalReport
- 🔄 集成Weather API

#### Week 11-12: 高级功能
- 🔄 AI聊天机器人（Google Gemini API）
- 🔄 测验系统（QR码）
- 🔄 3D热能可视化（Three.js）
- 🔄 预测功能（真实算法）

---

## 10. 快速参考

### 10.1 关键文件位置

| 功能 | 前端文件 | 后端文件 |
|------|----------|----------|
| 登录 | `LoginPage.jsx` | `userRoutes.js` |
| 用户管理 | `UserManagementPage.jsx` | `userController.js` |
| 碳足迹 | `CarbonFootprintPage.jsx` | `electricityController.js` |
| 权限控制 | `AccessControl.js` | `AccessControl.js` |

### 10.2 启动命令

**后端**:
```bash
cd ecosphere-backend
npm start
# 运行在 http://localhost:3001
```

**前端**:
```bash
cd ecosphere-frontend
npm run dev
# 运行在 http://localhost:5174
```

### 10.3 测试账号

- **Admin**: `admin.admin@edu.sait.ca` / `abcd1234`
- **TeamMember**: 通过Admin创建

---

## 11. 总结

### 11.1 架构优势

1. **清晰的分层** - 前端、后端、数据层职责明确
2. **组件化设计** - 易于维护和扩展
3. **前后端分离** - 独立开发和部署
4. **符合类图** - 严格遵循设计文档
5. **易于理解** - 逻辑清晰，文档完善

### 11.2 当前状态

- ✅ **P0功能**: 100%完成
- ⏳ **P1功能**: 0%待开始
- ⏸️ **P2功能**: 0% Skeleton

### 11.3 下一步

1. 实现电力仪表板模块（P1）
2. 准备数据库迁移（Week 1-2）
3. 实现Skeleton功能（P2）

---

## 📚 相关文档

- **项目介绍**: `1.EcoSphereIntroduction.md`
- **实施计划**: `3.IMPLEMENTATION_PLAN.md`
- **开发日志**: `log.md`
- **类图**: `diagram/class-diagram-optimized.puml`
- **ERD图**: `diagram/erd-diagram.puml`

---

**文档版本**: 1.0  
**最后更新**: 2025-11-28  
**更新者**: Kiro AI

---

**EcoSphere系统架构文档完成！** ✅
